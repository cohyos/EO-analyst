# Serve the UI against realistic Hebrew mock data instead of the live API.
# Default is off (false) so `npm run dev` talks to the real backend at
# http://127.0.0.1:8765 via the Vite proxy (see vite.config.ts).
VITE_USE_MOCKS=false
