# תוכנית מימוש — סוכן אנליסט EO (גרסה מקומית)
## שלב א' מורחב: תשתית מלאה ללא דחיות ארכיטקטוניות

**גרסה:** 1.1 | **מסמך בסיס:** מפרט דרישות v1.4 | **תאריך:** אוגוסט 2026
**הכרעות עיצוב (לפי סקירת הפישוטים):**
1. Apache AGE **מיום ראשון** — אין שכבת CTE זמנית
2. SearXNG + חיפוש עומק (FR-7) + רב-לשוני (FR-8) **במימוש מלא בשלב א'** — ממשק LLM מוכן-כלים מהתחלה
3. pgvector באותו PG — ארכיטקטורה סופית, ללא שינוי
4. שער משאבי חישוב (FR-14) — לפני כל קריאת מודל
5. מסלול ממשק מקביל: חלופות + אב-טיפוס Claude Design (FR-13) לפני מימוש UI

**יעד שלב א':** מחזור יומי מלא — איסוף ← שער אבטחה ← סיווג ← triage ← **חקירות עומק רב-לשוניות על 🔴** ← דוח יומי ← התראות; ואב-טיפוס ממשק מאושר.

---

## 1. מבנה הפרויקט (Repo)

```
eo-analyst/
├── docker-compose.yml
├── docker/postgres-age/Dockerfile   # PG17 + AGE + pgvector
├── install.sh
├── config/
│   ├── config.yaml
│   ├── sources.yaml
│   ├── watchlist.yaml
│   └── taxonomy.yaml
├── db/
│   ├── schema.sql                   # DDL טבלאי + וקטורי
│   └── graph_init.sql               # אתחול גרף AGE
├── agent/
│   ├── main.py                      # entrypoint + scheduler
│   ├── resources/                   # שער משאבים (FR-14)
│   ├── ingest/                      # RSS / HTML fetch
│   ├── security/                    # שער אבטחה (FR-9)
│   ├── pipeline/                    # דה-דופ, סיווג, triage, סיכום
│   ├── search/                      # SearXNG client + Deep Search ReAct (FR-7/8)
│   ├── llm/                         # ממשק Ollama עם tool-calling מלא
│   ├── memory/                      # PG: relational / graph (Cypher) / vector
│   ├── report/                      # דוח docx RTL
│   ├── notify/                      # ntfy
│   └── prompts/
├── design/                          # תוצרי FR-13: חלופות + קישורי אב-טיפוס
└── tests/
    └── fixtures/injection_samples/  # דגימות הזרקה לבדיקת FR-9
```

---

## 2. docker-compose — כל השירותים מיום ראשון

```yaml
services:
  postgres:
    build: ./docker/postgres-age     # PG17 + Apache AGE + pgvector באימג' אחד
    volumes: [pgdata:/var/lib/postgresql/data]
    environment: {POSTGRES_DB: eoanalyst}
  searxng:
    image: searxng/searxng
    volumes: [./config/searxng:/etc/searxng]
    # ללא פורט חשוף החוצה — גישה פנימית מה-agent בלבד
  ollama:
    image: ollama/ollama
    deploy: {resources: {reservations: {devices: [{driver: nvidia, count: 1, capabilities: [gpu]}]}}}
    volumes: [ollama:/root/.ollama]
  agent:
    build: ./agent
    depends_on: [postgres, searxng, ollama]
    volumes: [./config:/app/config, ./output:/app/output]
```

**מודלים:**
- ראשי: `gpt-oss:20b` — סיכום, ניתוח, דוח
- סוכן-חיפוש: `qwen3.6-35b-a3b` (MoE) — לולאת ה-ReAct של חקירות העומק (tool calling אמין)
- קל: `qwen3.5:9b` — סיווג, תיוג, NER, triage, תרגום שאילתות
- guard: `qwen3:4b` — סריקת הזרקות בפרומפט מבודד
- embeddings: `bge-m3`

*הערת VRAM: ארבעה מודלים לא חיים יחד ב-16GB — ניהול ה-swap הוא בדיוק תפקידו של שער המשאבים (סעיף 5), עם מדיניות מינימום-זמן-טעון למניעת thrashing.*

---

## 3. סכמת נתונים

### 3.1 טבלאי + וקטורי (schema.sql)
כפי שהוגדר בגרסה הקודמת של המסמך: `sources`, `items` (עם `embedding vector(1024)` ואינדקס HNSW), `entities`, `events`, `conferences`, `triage_feedback`, `lessons`, `security_log` — ובנוסף:

```sql
CREATE TABLE investigation_log (
  id           BIGSERIAL PRIMARY KEY,
  trigger_item BIGINT REFERENCES items(id),
  round        SMALLINT,               -- 1-4 לפי פרוטוקול ההתמדה
  lang         TEXT,
  query        TEXT,
  results_n    INT,
  pages_read   INT,
  outcome      TEXT,                   -- found | partial | not_found | stopped_budget
  created_at   TIMESTAMPTZ DEFAULT now()
);
CREATE TABLE search_playbook (
  id SERIAL PRIMARY KEY, pattern TEXT, strategy TEXT,
  success_count INT DEFAULT 1, last_used TIMESTAMPTZ
);
CREATE TABLE resource_log (              -- החלטות שער המשאבים (FR-14.5)
  id BIGSERIAL PRIMARY KEY, decision TEXT,   -- proceed | queued | deferred | swap
  model TEXT, vram_free_mb INT, gpu_util SMALLINT,
  wait_ms INT, created_at TIMESTAMPTZ DEFAULT now()
);
```

### 3.2 גרף — Apache AGE (graph_init.sql)
```sql
LOAD 'age';
SET search_path = ag_catalog, "$user", public;
SELECT create_graph('eo_graph');
-- קודקודים: Entity (מסונכרן מטבלת entities ע"י trigger)
-- קשתות: COMPETITOR_OF, SUPPLIER_OF, PARTNER_OF, ACQUIRED,
--         INTEGRATES_WITH, BIDS_AGAINST — כל קשת עם property: item_id (provenance)
```
כל הגישה לגרף דרך מודול `memory/graph.py` יחיד (שאילתות Cypher via `cypher('eo_graph', $$...$$)`), כך שהקוד האפליקטיבי לא תלוי בתחביר.

---

## 4. קונפיגורציה (תוספות עיקריות)

```yaml
deep_search:
  trigger_levels: [red]
  max_queries: 15
  max_pages: 30
  langs: [he, en, ru, zh, fr, de]      # מקביליות מלאה (FR-8)
  per_investigation_timeout_min: 25
searxng:
  url: http://searxng:8080
resources:                              # FR-14
  min_free_vram_mb: {main: 14000, search_agent: 13000, light: 7000, guard: 3500}
  queue_timeout_min: 20
  min_loaded_seconds: 300               # אנטי-thrashing
  polite_mode:
    external_gpu_util_threshold: 25     # אחוז ניצול שלא מהסוכן
    quiet_window: "23:00-06:00"
```

---

## 5. שער משאבי חישוב (FR-14) — לב התזמור

מודול `resources/gate.py`, נקודת מעבר יחידה לכל קריאת מודל:

```
request(model, est_vram) ►
  1. nvidia-smi: VRAM פנוי, ניצול GPU, האם יש עומס חיצוני לסוכן
  2. עומס חיצוני > סף? ► polite mode: משימות batch נדחות לחלון השקט
  3. VRAM מספיק?
       כן ► proceed (רישום ל-resource_log)
       לא, מודל אחר טעון ► עמד מינימום-זמן-טעון? ► swap ► proceed
       לא ► תור עם backoff (5s→10s→30s→60s) עד queue_timeout
  4. timeout ► דחיית המשימה למחזור הבא + לוג + ntfy
```

כל פונקציות הפייפליין מקבלות מודל דרך השער — אין קריאת Ollama ישירה בשום מקום אחר (נאכף ב-lint/בדיקה).

---

## 6. הפייפליין היומי

```
05:00  ingest        משיכת מקורות ► סניטציה ► guard scan ► items
05:30  dedup         embeddings ► cosine>0.92 מול 7 ימים ► dedup_of (חוצה-שפות)
05:45  classify      מודל קל: domain, dims, tags, entities, report_kind
06:15  triage        מודל קל: ציון 1-10 ► רמות
06:20  deep_search   לכל 🔴: לולאת ReAct (מודל סוכן-חיפוש):
                       ניסוח שאילתות ב-6 שפות במקביל ► SearXNG ►
                       בחירת תוצאות ► fetch ► Security Gate ► קריאה ►
                       סבבים 1-4 לפי פרוטוקול ההתמדה ► investigation_log
07:00  analyze       מודל ראשי: summary_he + חילוץ events + edges לגרף
07:30  report        דוח יומי md ► docx RTL ► QA סימוכין (חוסם [n] לא ממופה)
07:35  notify        ntfy: קישור לדוח + התראות 🔴 + תקציר חקירות
```

לוח הזמנים גמיש — כל שלב עובר דרך שער המשאבים, והמתזמן מריץ סדרתית לפי זמינות בפועל.

---

## 7. Backlog לביצוע ב-Claude Code

| # | משימה | תלות | קריטריון קבלה |
|---|---|---|---|
| T1 | אימג' postgres-age (PG17+AGE+pgvector), compose מלא, install.sh | — | `docker compose up` מרים הכל; `create_graph` רץ; SearXNG עונה פנימית |
| T2 | schema.sql + graph_init + seed watchlist + trigger סנכרון entities↔גרף | T1 | הוספת entity יוצרת קודקוד; שאילתת Cypher בסיסית עוברת |
| T3 | מודול llm/ עם tool-calling מלא + מודול resources/gate | T1 | בדיקת sanity לכלים עוברת; קריאה ללא VRAM נכנסת לתור ומתועדת ב-resource_log |
| T4 | ingest: RSS+HTML, סניטציה | T3 | 10 מקורות נמשכים; כשל מקור לא מפיל ריצה |
| T5 | שער אבטחה: guard + security_log + fixtures הזרקה | T4 | כל דגימות ההזרקה בתיקיית fixtures נחסמות ומתועדות |
| T6 | dedup + classify + triage (batch) | T5 | JSON תקין; 200 פריטים < 30 דק'; דה-דופ חוצה-שפות עובד על fixture |
| T7 | search/: SearXNG client + לולאת Deep Search ReAct + פרוטוקול התמדה + רב-לשוני | T3,T6 | חקירת בדיקה על אירוע ידוע מוצאת ≥3 מקורות; חקירה על אירוע פיקטיבי מסתיימת ב-not_found עם לוג מלא, בלי המצאות |
| T8 | analyze: סיכומים, events, כתיבת edges לגרף | T7 | אירוע עסקי יוצר גם רשומת events וגם קשת בגרף עם provenance |
| T9 | דוח docx RTL + QA סימוכין | T8 | docx תקין ב-Word; אפס טענות בלי [n]; חקירות עומק משתקפות בדוח |
| T10 | scheduler + watchdog + ntfy | T9 | מחזור מלא רץ מ-cron; התראות 🔴 וכשלים מגיעות |
| T11 | ריצת פיילוט 7 ימים + כיול | T10 | KPI רעש ודיוק נמדדים; עלות זמן-GPU יומית מתועדת |

**מסלול מקביל (לא חוסם את T1–T11):**
| D1 | מסמך חלופות ממשק (2–3 תפיסות, יתרונות/חסרונות) | — | המשתמש בוחר חלופה |
| D2 | אב-טיפוס עמוק ב-Claude Design: כל המסכים + פאנל סטטוס משאבים (FR-13.3), RTL, קליקבילי | D1 | אישור משתמש — האב-טיפוס הופך למפרט העיצוב של שלב ב' |

---

## 8. הגדרת "גמור" לשלב א'

1. מחזור יומי מלא **כולל חקירות עומק רב-לשוניות** רץ 7 ימים רצופים ללא התערבות
2. דוח docx יומי תקין (RTL, תקציר, פרקים, סימוכין) ב-07:35
3. שאילתת גרף Cypher ("כל השותפים של מתחרי X") מחזירה תשובה נכונה על נתוני הפיילוט
4. כל דגימות ההזרקה נחסמות; אפס הפעלת כלים מתוכן שנשלף
5. שער המשאבים: אפס קריסות OOM בכל הפיילוט; polite mode מוכח (עבודה על המחשב דוחה batch)
6. אב-טיפוס Claude Design מאושר
7. שחזור ממחשב נקי: install.sh + dump < 45 דקות (עלה מ-30 בגלל האימג' המותאם והמודל הנוסף)

---

## 9. שלבים הבאים
- **שלב ב':** Web UI לפי האב-טיפוס (כולל פאנל המשאבים החי), FR-10 הבהרות, FR-11 משוב, ייצוא Obsidian
- **שלב ג':** מכרזים/פטנטים, לוח כנסים (FR-12), פרופילי ישויות, דוחות שבועי/חודשי
- **שלב ד':** כיול מתמשך, הרחבת watchlist, זיהוי אנומליות
