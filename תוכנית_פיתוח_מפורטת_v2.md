# תוכנית פיתוח מפורטת — סוכן אנליסט EO/CV ביטחוני (גרסה 2.0)

**תאריך:** 4 בספטמבר 2026 | **מסמכי בסיס:** מפרט דרישות v1.4, תוכנית מימוש שלב א' v1.1
**סטטוס:** טיוטה להצגה ולאישור לפני תחילת פיתוח
**עיקרון מנחה חדש:** *אנליסט במשרה מלאה, בעלות מודלים אפס* — כל ההיסק מקומי, כל המקורות חינמיים, המחשב עובד בשבילך בלילה ועונה לך ביום.

---

## 0. תקציר מנהלים

**מה השתנה מול המסמכים הקיימים**

1. **החומרה בפועל שונה מהמפרט.** הכרטיס הוא RTX 5070 Ti **Laptop** עם **12 GB VRAM**, לא 16 GB. לעומת זאת יש 64 GB RAM ומעבד Core Ultra 9 275HX עם 24 threads. זה פוסל חלק מהמודלים שתוכננו ומחייב מדיניות VRAM שמרנית יותר, אבל פותח אפשרות ל-offload של מודלי MoE לזיכרון המערכת.
2. **דרישת "מודלים מערביים בלבד"** פוסלת את Qwen 3.5/3.6 (Alibaba), DeepSeek ואת מודל ה-embeddings bge-m3 (BAAI, בייג'ינג) שהופיעו בשני המסמכים. ההחלפות המוצעות בפרק 2.
3. **הצמצום העיקרי:** במקום חמישה מודלים שמתחלפים ב-VRAM, ארכיטקטורה של **מודל תושב אחד** (12B) לכל משימות השפה, מודל שומר זעיר על CPU, ו-embeddings קל. פחות החלפות = פחות סיכון קריסה, פחות זמן טעינה, פחות מורכבות.
4. **חלון העבודה הלילי** מוגדר מחדש: 01:00–06:00 שעון ישראל (Asia/Jerusalem, כולל שעון קיץ) הוא חלון ה-batch. הרצה יזומה אפשרית בכל שעה תחת "מצב מנומס".
5. **הגנה מקריסה** הופכת לתת-מערכת מפורשת בארבע שכבות: מכסות WSL2, מכסות קונטיינרים, שער משאבים לפני כל היסק, ו-watchdog עם circuit breaker.
6. **הרחבת תחומי הכיסוי** למטע"די תצפית יבשתיים (גבולות, מגדלי תצפית, רק"ם), ימיים (ספינות, חופים, תורני צוללות) ומטע"די תצפית לכטב"מים אסטרטגיים.
7. **ממשק אדם-מכונה** מקבל פרק מלא: תפיסה, מסכים, מערכת עיצוב RTL, מחסנית טכנולוגית ותהליך אב-טיפוס.
8. **בדיקות** מקבלות פרק מלא, כולל מערך הערכת איכות LLM (eval harness) עם סט זהב בעברית.

**סקר הסביבה שבוצע היום (4.9.2026)**

| רכיב | מצב בפועל |
|---|---|
| GPU | RTX 5070 Ti Laptop, 12,227 MiB VRAM, דרייבר 591.91 |
| RAM / CPU | 64 GB / Core Ultra 9 275HX, 24 threads |
| דיסק C: | ~1.88 TB בשימוש, **~123 GB פנויים בלבד** |
| Ollama | 0.33.2 מותקן נייטיבית; תיקיית מודלים 186 GB |
| Docker Desktop | 29.6.2, WSL2 backend, nvidia runtime זמין; **GPU passthrough לקונטיינר אומת** |
| WSL2 | 2.7.10, אין קובץ .wslconfig (ברירת מחדל: עד 50% RAM) |
| Python / Node | 3.14.4 / 24.15 (python-docx, psycopg, lxml מותקנים) |
| חשמל | על AC: שינה = לעולם לא, היברנציה = לעולם לא, wake timers מופעלים. על סוללה: שינה אחרי 180 דק' |
| אזור זמן | Israel Standard Time |

מודלים מערביים שכבר מותקנים ב-Ollama: gemma4:12b, gemma4:e4b, gemma4:e2b (Google), gpt-oss:20b (OpenAI), nemotron-3.5-lightning (NVIDIA, 32.9B MoE), DictaLM-3.0-Nemotron-12B-Instruct ו-DictaLM-3.0-24B-Thinking (Dicta, ישראל).
מודלים שיש להסיר (מקור סיני, ~130 GB): qwen3-coder-next, qwen3.6:35b-a3b, qwen3-coder:30b ×2, qwen3:14b, deepseek-r1:14b, bge-m3.

---

## 1. ביקורת המסמכים הקיימים

### 1.1 מפרט הדרישות v1.4 — ממצאים

| # | ממצא | חומרה | תיקון מוצע |
|---|---|---|---|
| A1 | הנחת 16 GB VRAM (כותרת, NFR-2, 8.1) שגויה — בפועל 12 GB | גבוהה | לעדכן ל-12 GB; תקרת מודל ≤ 9.5 GB כולל KV cache; לנצל 64 GB RAM ל-offload |
| A2 | מודלים סיניים (Qwen3.6, Qwen3.5, bge-m3) סותרים דרישת "מערבי בלבד" שלא נכתבה במסמך | גבוהה | להוסיף NFR-13: "מקור מודלים" — רק ספקים מארה"ב/אירופה/ישראל, רישיון פתוח, פורמט GGUF/safetensors בלבד |
| A3 | חמישה מודלים על כרטיס אחד — thrashing מובנה | גבוהה | ארכיטקטורת מודל תושב אחד (פרק 2) |
| A4 | תחומי הכיסוי (2.1) מתמקדים בפודים אוויריים; חסרים מטע"די תצפית יבשתיים וימיים | בינונית | להוסיף שורה "מטע"די תצפית ומעקב יבשתיים וימיים" (ראו 1.3) |
| A5 | לוח זמנים: המסמך שותק; תוכנית המימוש קובעת 05:00–07:35 — מתנגש עם שעות הבוקר | בינונית | חלון batch 01:00–06:00 שעון ישראל (פרק 5) |
| A6 | FR-9.2: "מודל קל 3–4B" לסריקת הזרקות על כל פריט — יקר מדי ופחות מדויק ממסווג ייעודי | בינונית | שכבה ראשונה: מסווג הזרקות ייעודי זעיר על CPU; LLM מבודד רק לחשודים |
| A7 | ארכיטקטורה (פרק 8) אומרת "דוחות Markdown/HTML" בעוד FR-5.1 דורש docx — סתירה | נמוכה | docx הוא התוצר הרשמי; Markdown הוא ביניים; HTML לתצוגה בממשק |
| A8 | FR-6.2: תלות ב-Apache AGE ללא תוכנית אם AGE מפגר אחרי גרסת PG | בינונית | להצמיד גרסת PG לגרסה ש-AGE תומך בה רשמית; חלופה מוכנה: טבלאות nodes/edges + CTE רקורסיבי |
| A9 | אין דרישה על **דיסק** (מודלים + PG + דוחות + cache) — בפועל נשארו 123 GB | בינונית | NFR-14: ≥ 150 GB פנויים; watchdog דיסק; ניקוי מודלים לא-מערביים |
| A10 | אין דרישה על **חום** — מחשב נייד ב-batch של 5 שעות | בינונית | FR-14 מקבל סעיף תרמי: השהיה מעל 83°C GPU / throttle |
| A11 | NFR-12: "התקנה ב-45 דקות" תלוי במהירות הורדת ~25 GB מודלים | נמוכה | לנסח כ"התקנה ללא הורדות < 20 דק'; הורדת מודלים תלויה ברוחב פס" + אפשרות bundle offline |
| A12 | KPI-1 "≥90% מהאירועים המשמעותיים" אינו מדיד ללא ground truth | נמוכה | להגדיר מדגם שבועי ידני של 10 אירועים (מהעיתונות) ולמדוד recall מולו |
| A13 | אין סעיף **עלות**: העיקרון "אפס עלות מודלים" ראוי להיות NFR מפורש | בינונית | NFR-15: אפס API בתשלום בזמן ריצה; ntfy ו-SearXNG בהרצה עצמית או בשירות חינמי |
| A14 | אין מדיניות **שמירת נתונים** (retention) ו**גיבוי** מתוזמן | בינונית | גיבוי pg_dump לילי + שמירה 30 יום; retention לטקסט גולמי 90 יום, לסיכומים ללא הגבלה |
| A15 | אין סעיף **תצפיתיות** (logs/metrics) מעבר ללוג שגיאות | נמוכה | structlog JSON + טבלת run_log + פאנל סטטוס (כבר ב-FR-13.3) |
| A16 | FR-8: שש שפות במקביל × 15 שאילתות = עומס SearXNG וזמן — לא נבחן מול תקציב הלילה | בינונית | תקציב זמן לחקירה (25 דק') ותקרת חקירות ללילה (ברירת מחדל 4); שפות משניות רק בסבב 3+ |
| A17 | 4.1: "שחקנים סיניים — למעקב מודיעין תחרותי בלבד" — ניסוח ראוי, אך יש להבהיר שמקורות סיניים נקראים דרך שער האבטחה כמו כל מקור | נמוכה | הערה ב-5.4 |

### 1.2 תוכנית המימוש v1.1 — ממצאים

| # | ממצא | תיקון מוצע |
|---|---|---|
| B1 | ארבעה/חמישה מודלים כולל Qwen; הערת VRAM מודה בבעיה | ראו פרק 2 |
| B2 | `embedding vector(1024)` מניח bge-m3 | הממד נגזר ממודל ה-embedding שייבחר (768 או 1024); לקבוע ב-config ולא ב-DDL קשיח |
| B3 | polite_mode: quiet_window "23:00-06:00" | ליישר ל-"01:00-06:00" (חלון batch) והגדרת "polite" לשעות היום |
| B4 | פייפליין 05:00–07:35 | 01:00–05:45 (פרק 5) עם deadline קשיח 06:00 |
| B5 | T7 "חקירה על אירוע פיקטיבי מסתיימת ב-not_found" — קריטריון מצוין, יש להפוך אותו לבדיקת רגרסיה קבועה | נכלל בפרק 9 |
| B6 | אין משימת Phase 0 (הכנת סביבה, bake-off מודלים, ניקוי דיסק) | נוסף כ-Phase 0 |
| B7 | אין משימות בדיקה נפרדות; "תיקיית fixtures" בלבד | פרק 9 מלא |
| B8 | D1/D2 (ממשק) "לא חוסם" — אבל שלב ב' תלוי בהם; ראוי להתחיל D1 בשבוע הראשון | פרק 8 + לו"ז |
| B9 | אין הגדרה מה קורה כשהמחזור הלילי לא הסתיים עד 06:00 | מנגנון deadline + carry-over (5.3) |
| B10 | הסכמה חסרה טבלאות `run_log`, `jobs` (תור משימות persist), `model_registry` (digest של כל מודל) | נוסף ל-DDL |

### 1.3 הרחבת תחומי הכיסוי (לפי בקשתך)

להוסיף לטבלה 2.1 במפרט:

| תחום | תתי-נושאים |
|---|---|
| **מטע"די תצפית ומעקב יבשתיים** | מערכות תצפית לגבולות ולהגנת גדר (Long-Range EO/IR Surveillance, Border Towers), מערכות תצפית ניידות ונישאות, מטע"די תצפית לרק"ם ולכלי רכב (Commander/Gunner Sights, Situational Awareness), אמצעי ראיית לילה אישיים (Fused NV, Digital Night Vision), מכ"ם+EO משולבים לאבטחת מתקנים |
| **מטע"די תצפית ימיים** | מערכות EO/IR לספינות (Naval EO Directors, Surveillance & Fire-Control), תצפית חופית ונמלים (Coastal Surveillance), תורני צוללות אופטרוניים (Optronic Masts), מטע"דים לכלי שיט בלתי מאוישים (USV), ראיית ים ארוכת-טווח בתנאי ערפל ולחות |
| **מטע"די תצפית לכטב"מים אסטרטגיים** | מטע"די ISR רב-חיישניים (EO/IR/Laser/HD), Wide-Area Motion Imagery (WAMI), Hyperspectral, SAR+EO fusion — בהקשר של המטע"ד בלבד |

ולהוסיף ל-watchlist (4.1): Controp, Elbit Land (Israel); Hensoldt Optronics, Safran Vectronix, Thales Land & Naval, Leonardo DRS (land), Teledyne FLIR Defense (land/maritime), L3Harris WESCAM, Rheinmetall (naval directors), Saab (naval EO), FLIR Systems maritime, Sagem/Safran Paseo, Elbit ISTAR (naval), Terma (coastal), Aselsan (land/naval EO).

---

## 2. הכרעות מודלים — מערביים בלבד, 12 GB VRAM

### 2.1 עקרונות

1. **מקור מערבי בלבד:** ספקים מארה"ב, האיחוד האירופי, בריטניה, שווייץ, קנדה, ישראל. רשימה לבנה ב-`config/models.yaml`; מודל שאינו ברשימה לא נטען (נאכף ב-CI ובזמן ריצה).
2. **פורמט משקלים בטוח:** GGUF או safetensors בלבד — לעולם לא pickle/.bin. כל מודל נרשם ב-`model_registry` עם SHA-256 digest, גרסה, מקור ורישיון; שינוי digest = חסימה.
3. **מודל תושב אחד:** מודל 12B אחד טעון כל הלילה עושה סיווג, triage, סיכום, ניתוח, ReAct של חיפוש וכתיבת דוח. אין החלפות בין שלבים.
4. **הבחנה "היסק" מול "כתיבה בעברית":** אם ה-bake-off יראה שמודל ישראלי (DictaLM 3.0) כותב עברית רהוטה יותר, הוא ישמש כ"עורך לשוני" רק בשלב הדוח (החלפה אחת בלילה, בסופו).
5. **מודל כבד = אופציונלי:** gpt-oss:20b (MoE, ~3.6B פעילים) יכול לרוץ עם offload חלקי ל-RAM. נשמר כאופציה לחקירות עומק אם יוכח יתרון ב-bake-off. Nemotron 3.5 Lightning (32.9B MoE) — כנ"ל, יקר יותר.

### 2.2 טבלת המודלים המוצעת (לאחר אימות מקוון, 4.9.2026)

| תפקיד | מודל מוצע | ספק / מדינה | רישיון | גודל (Q4) | VRAM בפועל | עברית | חלופה / מתמודד ב-bake-off |
|---|---|---|---|---|---|---|---|
| **מודל תושב** — סיווג, triage, סיכום, ניתוח, ReAct, דוח | **gemma4:12b** (dense, מותקן) | Google / ארה"ב | Apache 2.0 | 6.7–7.6 GB | ~9.5 GB עם 16k ctx, KV q8 | 140+ שפות, עברית כלולה (לאמת ב-bake-off) | **gemma4:26b** (MoE 26B-A4B, 14.4 GB, 4B פעילים — offload ל-RAM; איכות 26B במחיר חישוב של 4B) |
| **עורך לשוני עברי** (אופציונלי, שלב דוח בלבד) | **DictaLM-3.0-Nemotron-12B-Instruct** (מותקן) | Dicta / ישראל; בסיס NVIDIA Nemotron Nano V2 | פתוח (נוסח רישיון לאימות) | 7.5 GB | ~9 GB | ייעודית; דו"ח טכני מראה תחרותיות מול Gemma-3-12B | DictaLM-3.0-24B (בסיס Mistral Small 3.1, 14 GB — offload) |
| **מודל חקירה כבד** (אופציונלי, רק אם bake-off יראה יתרון) | gpt-oss:20b (מותקן) | OpenAI / ארה"ב | Apache 2.0 | 14 GB MXFP4, 3.6B פעילים | ~11 GB GPU + offload | בינונית | nemotron-3.5-lightning (30B/3B פעילים, 25 GB, OpenMDW-1.1, 1M ctx, 97.7% דיוק tool-call) |
| **מודל קל לפעולות יום** (שאילתות מהירות, polite mode) | **gemma4:e4b** (מותקן, 4.5 GB) | Google | Apache 2.0 | 4.5 GB | ~6 GB | סבירה | phi4-mini (Microsoft, MIT, 2.5 GB, עברית חלשה) |
| **שומר הזרקות — שכבה 1** (כל פריט, CPU) | **Prompt Guard 2 — 86M** | Meta / ארה"ב | Llama license | 0.2 GB | CPU בלבד | מסווג דפוסים, לא תלוי שפה | גרסת 22M (מהירה יותר, פחות מדויקת) |
| **שומר הזרקות — שכבה 2** (רק חשודים) | **Granite Guardian 4.1 — 2B/3B** | IBM / ארה"ב | Apache 2.0 | 1.5–2 GB | CPU או GPU בין שלבים | תומך בקריטריונים מותאמים (BYOC) | gemma4:e2b בפרומפט מבודד ללא כלים |
| **Embeddings** | **multilingual-e5-large-instruct** | Microsoft / ארה"ב | MIT (לאמת — סוכן האימות דיווח גם CC-BY-SA) | 1.1 GB (560M) | ~1.2 GB או CPU | **עברית מפורשת** (94+ שפות); בספריית Ollama | EmbeddingGemma-300M (Google, 768-dim); nomic-embed-text-v2-moe (Apache 2.0, עברית לא מאומתת) |
| **Reranker** (אופציונלי, RAG) | jina-reranker-v3 (0.6B) | Jina AI / גרמניה | לא-מסחרי (לאימות; שימוש אישי) | 1.2 GB | CPU | נבדק על עברית (MKQA) | mxbai-rerank-large-v2 (MIT, אנגלית בעיקר) |

**מודלים שנפסלו במפורש:** Qwen 3.x (Alibaba), DeepSeek, bge-m3 (BAAI), **DictaLM-3.0-1.7B** (למרות היותו ישראלי, הבסיס שלו Qwen3-1.7B), Llama Guard 4 (12B — כבד מדי לצד המודל התושב).

*הערות:* (א) gemma4:12b תומך ב-tools, thinking ו-vision — היכולת החזותית מאפשרת קריאת עלוני מוצר (datasheets) ותמונות מתערוכות. (ב) חלון ההקשר 256k תיאורטי; בפועל נגביל ל-16k–32k. (ג) ממד הווקטור נגזר מבחירת ה-embeddings: 1024 ל-e5, 768 ל-EmbeddingGemma — נקבע ב-config. (ד) Mistral Small 3.1/4 (24–30B, ~15 GB) ו-Ministral 3 (3B/8B/14B, Apache 2.0, vision) נשקלו; Ministral-3-14B הוא מתמודד לגיטימי ב-bake-off אם Gemma יאכזב בעברית.

### 2.3 תקציב VRAM (12,227 MiB)

```
מערכת + מסך (Windows, דרייבר)         ~ 600 MiB   (נמדד: 486 MiB במנוחה)
מרווח בטיחות קבוע                      1,200 MiB
Embeddings (ברירת מחדל על CPU)               0 MiB
מודל תושב 12B Q4_K_M                    7,700 MiB
KV cache 16k tokens, q8_0               ~1,200 MiB
────────────────────────────────────────────────
סה"כ                                   ~10,700 MiB   ← עומד במגבלה (מרווח ~1.5 GB)
```
כללים: `OLLAMA_MAX_LOADED_MODELS=1`, `OLLAMA_NUM_PARALLEL=1`, `OLLAMA_KV_CACHE_TYPE=q8_0`, `OLLAMA_FLASH_ATTENTION=1`, `num_ctx` לפי משימה (סיווג 4k, סיכום 8k, ReAct 16k, דוח 32k עם בדיקת VRAM לפני). embeddings על CPU כשמודל השפה טעון ב-32k.

### 2.4 ארכיטקטורת אי-דליפה (Data-Leak Prevention)

מודל פתוח בקובץ GGUF אינו יכול "להתקשר הביתה" — הוא משקלים סטטיים. וקטורי הדליפה האמיתיים הם **הרנטיים (runtime), הרשת והשרשרת האספקה**. לכן:

**שכבה 1 — בידוד רשת (Docker)**
```
network "egress"   (יוצאת לאינטרנט):  searxng, fetcher
network "internal" (internal: true):   postgres, ollama, worker, web-ui, scheduler
```
- `ollama`, `worker` (המודל המעבד תוכן גולמי) ו-`postgres` **ללא כל גישה לאינטרנט**. גם אם מודל "ירצה" לדלוף — אין נתיב.
- `fetcher` הוא קונטיינר מינימלי (Python + httpx + trafilatura) שרק מוריד, מסנן (FR-9.1) ושומר טקסט נקי ל-PG. אין לו גישה למודלים.
- `searxng` נגיש רק מ-`fetcher`; פורט לא חשוף למחשב.
- `web-ui` נגיש רק מ-localhost (bind 127.0.0.1) — לא מהרשת המקומית.

**שכבה 2 — הקשחת מארח (Windows)**
- אם Ollama רץ נייטיבית (ראו 3.3): כלל Windows Firewall יוצא **חוסם** ל-`ollama.exe` ול-`ollama_llama_server.exe` (הורדת מודלים מתבצעת ידנית בפקודת `install`, עם פתיחה זמנית).
- Docker Desktop: כיבוי "Send usage statistics"; ללא Docker Scout; ללא Extensions.
- Ollama: `OLLAMA_HOST=127.0.0.1` בלבד; אין טלמטריה (לאמת בסעיף 2.5).

**שכבה 3 — שרשרת אספקה של מודלים**
- הורדה רק מ-ollama.com/library או מארגונים רשמיים ב-Hugging Face (google, openai, meta-llama, nvidia, mistralai, dicta-il, ibm-granite, nomic-ai).
- `models.lock` עם digest; `install` מוודא; ריצה מסרבת לטעון מודל שאינו ב-lock.
- Bundle offline: תיקיית `models/` הניתנת להעתקה למחשב אחר ללא אינטרנט.

**שכבה 4 — הפרדת הרשאות (FR-9.4 מחוזק)**
- `worker` (קורא תוכן גולמי) — **ללא כלים**, ללא רשת, פלט JSON בלבד לפי סכמה.
- `orchestrator` — קורא רק פלט JSON מאומת מה-DB, מפעיל כלים (חיפוש דרך fetcher, כתיבה ל-DB, ntfy).
- ה-ReAct של חקירת העומק פועל על **סיכומי fetcher מסוננים** בלבד, לעולם לא על HTML גולמי.

**שכבה 5 — סודות ונתוני משתמש**
- אין מפתחות API בכלל (עיקרון אפס עלות). טוקן ntfy (אם ערוץ פרטי) ב-`.env` מחוץ ל-git.
- המאגר מכיל מקורות גלויים בלבד; שדה `classification=OSINT` חובה בכל רשומה; אין יבוא קבצים מקומיים ללא אישור מפורש.

**מודל איומים תמציתי**

| איום | מענה |
|---|---|
| תוכן שנשלף מנסה להפעיל כלים | worker ללא כלים; DATA-framing; Prompt Guard |
| מודל מנסה לפנות לאינטרנט | רשת internal; firewall |
| משקלים מזויפים/מורעלים | digest lock; מקורות רשמיים בלבד; GGUF/safetensors |
| Ollama/Docker מדווחים טלמטריה | כיבוי; אימות תעבורה בבדיקת קבלה (Wireshark/pktmon 24 שעות) |
| דליפה דרך שאילתות חיפוש | SearXNG מקומי, ללא חשבון, רוטציית מנועים |
| דליפה דרך דוחות שנשלחים ל-ntfy | ntfy מקבל רק כותרת + קישור מקומי; לא תוכן הדוח |

### 2.5 תוצאות האימות המקוון (4.9.2026, שני סוכני מחקר)

**מודלי שפה**
- **Gemma 4** (Google, 2.4.2026, Apache 2.0): E2B, E4B, 12B (dense), **26B-A4B** (MoE, 4B פעילים), 31B (dense). הקשר 128k (E) / 256k (12B+). tool calling נייטיבי. 140+ שפות. גדלים ב-Ollama: e2b 2.9 GB, e4b 4.5 GB, 12b 6.7 GB, 26b 14.4 GB, 31b 17.5 GB. *הגרסה המותקנת אצלך (7.6 GB) היא כנראה קוונטיזציה מעט שונה — לעדכן ב-P0.2.*
- **Nemotron 3.5 Lightning** (NVIDIA, 11.8.2026, OpenMDW-1.1): 30B/3B פעילים, Mamba-2 + MoE, 1M הקשר, 25 GB Q4, שותף השקה של Ollama. חזק ב-tool calling; גדול מדי כתושב, אפשרי עם offload לחקירות.
- **gpt-oss-20b** (OpenAI, Apache 2.0): 21B/3.6B פעילים, 14 GB MXFP4, reasoning_effort low/medium/high, tool calling (Harmony). offload חלקי אפשרי על 12 GB.
- **DictaLM 3.0** (Dicta, ישראל, דו"ח טכני arXiv 2602.02104): 24B על בסיס Mistral-Small-3.1, **12B על בסיס NVIDIA Nemotron Nano V2**, 1.7B על בסיס Qwen3 (נפסל). הקשר 65k. tool calling. benchmark עברי ייעודי (תרגום, סיכום, ניקוד, טריוויה ישראלית). זמין כ-GGUF ב-Hugging Face (dicta-il), לא בספריית Ollama הרשמית — כבר מותקן אצלך דרך hf.co.
- **Mistral**: Small 3.1 (24B, Apache 2.0, ~15 GB), Small 4 (מרץ 2026, MoE ~30B/6B פעילים), **Ministral 3** (3B/8B/14B, Apache 2.0, vision).
- **Microsoft**: Phi-4-mini 3.8B (MIT, 2.5 GB, 15+ שפות); Phi-5 טרם שוחרר.

**Embeddings / Reranker**
- multilingual-e5-large-instruct (Microsoft): 1024-dim, 94+ שפות **כולל עברית**, בספריית Ollama. נבחר.
- EmbeddingGemma-300M (Google): 768-dim, 100+ שפות, רישיון Gemma. גיבוי.
- nomic-embed-text-v2-moe (Apache 2.0), snowflake-arctic-embed2 (Apache 2.0, 74 שפות) — עברית לא מאומתת.
- Jina Reranker v3 (גרמניה, 0.6B): נבדק על עברית; רישיון לא-מסחרי — מתאים לשימוש אישי.

**שומרים**
- Prompt Guard 2 (Meta): 22M / 86M, BERT-style, זיהוי הזרקות ו-jailbreak, זמן אמת על CPU.
- Granite Guardian 4.1 (IBM, Apache 2.0): 2B/3B/5B/8B, כולל jailbreak, הלוצינציות וקריטריונים מותאמים (BYOC).
- Llama Guard 4 (12B, מולטימודלי) — כבד מדי; ShieldGemma 2 — תמונות בלבד.

**Ollama 0.33.x**
- ללא טלמטריה על תוכן; תעבורה יוצאת יחידה: בדיקת גרסה (ניתן לחסום ב-firewall). **`OLLAMA_NO_CLOUD=1`** מכבה יכולות ענן — חובה בקונפיגורציה שלנו.
- משתני VRAM: `OLLAMA_MAX_LOADED_MODELS`, `OLLAMA_KEEP_ALIVE`, `OLLAMA_NUM_PARALLEL`, `OLLAMA_GPU_OVERHEAD` (מומלץ 10–15% מה-VRAM ≈ 1.2 GB), `OLLAMA_FLASH_ATTENTION=1`, `OLLAMA_KV_CACHE_TYPE=q8_0`, `OLLAMA_CONTEXT_LENGTH`, ו-`num_gpu` לשליטה ידנית בשכבות. offload חלקי ל-MoE מתבצע אוטומטית.

**תשתית**
- Docker Desktop + WSL2 + GPU: נתמך רשמית; אומת מקומית היום. דיווחי ביצועים: 90–100% מנייטיבי ב-WSL2 CLI, אך ירידה של 5–10% ב-VRAM נגיש (שכבת dxcore) ודיווחים על האטה ניכרת דרך Docker Desktop למודלים גדולים — **P0.4 מכריע במדידה**.
- `.wslconfig`: `memory`, `processors`, `swap` תחת `[wsl2]`; `autoMemoryReclaim=gradual` תחת `[experimental]` (Windows 11, WSL ≥ 2.0). נכנס לתוקף אחרי `wsl --shutdown`.
- Apache AGE 1.6.0 (ינואר 2026) תומך ב-**PostgreSQL 17**; PG 18 בפיתוח. אין אימג' רשמי משולב AGE+pgvector — נבנה Dockerfile משלנו על בסיס `apache/age` (PG17) עם קומפילציית pgvector.

---

## 3. סביבת הפיתוח וההרצה

### 3.1 מארח
- Windows 11 Home, Docker Desktop (WSL2), דרייבר NVIDIA עם תמיכת CUDA ב-WSL (אומת).
- `%USERPROFILE%\.wslconfig` — **ליצור** (כיום חסר):
```ini
[wsl2]
memory=36GB          # משאיר 28 GB ל-Windows ולעבודה שלך ביום
processors=16        # מתוך 24
swap=8GB

[experimental]
autoMemoryReclaim=gradual
```
(נכנס לתוקף אחרי `wsl --shutdown`.)
- Docker Desktop → Resources: מכסה כוללת נגזרת מ-.wslconfig; כיבוי Resource Saver בלילה (מפריע לג'ובים).
- חשמל: להשאיר את הגדרות ה-AC הקיימות (ללא שינה). להוסיף Task Scheduler "EO-Analyst Wake" ב-00:55 עם "Wake the computer to run this task" כגיבוי, ו-`powercfg /requestsoverride` לתהליך ה-agent כדי שלא ייכנס ל-Modern Standby באמצע ריצה.
- דיסק: **לפני הכל** — ניקוי ~130 GB של מודלים לא-מערביים (פקודת `ollama rm`, באישורך), יעד ≥ 200 GB פנויים; להעביר את תיקיית המודלים ל-`D:` אם קיים.

### 3.2 מחסנית טכנולוגית

| שכבה | בחירה | נימוק |
|---|---|---|
| שפה | Python 3.12 בקונטיינר (לא 3.14 — ספריות ML/psycopg-binary עוד לא יציבות שם) | יציבות |
| ניהול תלויות | `uv` + `pyproject.toml` + lockfile | מהיר, דטרמיניסטי |
| DB | PostgreSQL בגרסה ש-Apache AGE תומך רשמית + pgvector, אימג' מותאם | דרישת FR-6 |
| גישה ל-DB | psycopg 3 + SQLAlchemy Core (לא ORM כבד); מיגרציות עם Alembic | שקיפות SQL |
| LLM | Ollama API (`/api/chat` עם tools, `format: json schema`) דרך מודול `llm/` יחיד | אכיפת שער משאבים |
| וידוא פלט | Pydantic v2 סכמות לכל פלט מודל; retry עם תיקון | אפס JSON שבור |
| איסוף | feedparser, httpx, trafilatura, readability-lxml, playwright (רק לאתרים דינמיים, בקונטיינר fetcher) | |
| תזמון | APScheduler (in-process) + טבלת `jobs` persist | שרידות לקריסה |
| דוחות | python-docx + lxml (RTL, סגנונות, TOC), תבנית `.dotx` | דרישת FR-5 |
| התראות | ntfy (self-hosted בקונטיינר או ntfy.sh ערוץ פרטי) | חינם |
| Web UI | FastAPI + WebSocket (backend); React 19 + TypeScript + Vite + Tailwind (RTL) + shadcn/ui; Cytoscape.js (גרף); Recharts (מדדים) | פרק 8 |
| לוגים | structlog → JSON → קובץ + טבלת `run_log`; Loki אופציונלי | |
| איכות קוד | ruff, mypy (strict על מודולי ליבה), pre-commit, pytest, testcontainers, Playwright | |

### 3.3 Ollama: נייטיבי או בקונטיינר?

| | Ollama נייטיבי ב-Windows (כיום) | Ollama בקונטיינר (WSL2 + GPU) |
|---|---|---|
| ביצועים | מלאים | מלאים (אומת passthrough); עומס I/O מודל דרך 9p/virtiofs |
| בידוד רשת | דורש כלל firewall ידני | מובנה (`internal: true`) |
| ניידות (NFR-12) | דורש התקנה נפרדת | `docker compose up` בלבד |
| polite mode | nvidia-smi מהמארח — קריאה ישירה | nvidia-smi בתוך קונטיינר מציג GPU כולו — עובד |
| סיכון קריסה | Ollama יכול לקחת VRAM מעבר למכסה | מכסות cgroup ל-RAM/CPU; VRAM עדיין באחריות השער |

**החלטה (מותנית במדידה ב-P0.4):** ברירת המחדל המומלצת לאחר האימות היא **Ollama נייטיבי ב-Windows** — גישה ישירה ל-GPU ללא שכבת dxcore, 5–10% יותר VRAM נגיש (קריטי ב-12 GB), והתקנה פשוטה יותר. הקונטיינרים פונים אליו דרך `host.docker.internal:11434`. הבידוד מושג ב-Windows: `OLLAMA_HOST=127.0.0.1`, `OLLAMA_NO_CLOUD=1`, וכלל Firewall יוצא חוסם ל-`ollama.exe` (נפתח זמנית רק בפקודת `install --pull-models`). ה-`worker` נשאר ברשת `internal` ללא אינטרנט בכל מקרה. פרופיל compose `--profile container-ollama` נשמר לניידות מלאה (NFR-12) ולמחשבי Linux. אם P0.4 יראה ש-Ollama בקונטיינר מגיע ל-≥ 90% מהביצועים ו-≥ 95% מה-VRAM הנגיש — ההחלטה מתהפכת לטובת הקונטיינר.

### 3.4 מבנה הרפו (עדכון)

```
eo-analyst/
├── docker-compose.yml, docker-compose.override.example.yml
├── docker/ postgres-age/, fetcher/, worker/, web/
├── install.ps1 / install.sh          # בדיקות קדם, .wslconfig, הורדת מודלים + digest
├── config/ config.yaml, sources.yaml, watchlist.yaml, taxonomy.yaml, models.yaml, models.lock
├── db/ migrations/ (Alembic), graph_init.sql, seed/
├── agent/
│   ├── orchestrator/   scheduler, jobs, deadline, night-window
│   ├── resources/      gate.py, gpu.py (nvidia-smi), thermal.py, disk.py
│   ├── fetch/          (רץ בקונטיינר fetcher) rss, html, sanitize
│   ├── security/       prompt_guard.py, heuristics.py, quarantine.py
│   ├── pipeline/       dedup, classify, triage, analyze
│   ├── search/         searxng_client, react_loop, persistence_protocol, multilingual
│   ├── llm/            ollama_client (הנקודה היחידה), schemas/, prompts/
│   ├── memory/         relational, graph (Cypher), vector, agent_memory
│   ├── report/         daily.py, docx_builder.py, qa_citations.py, templates/
│   └── notify/         ntfy.py
├── web/                React app
├── evals/              golden sets (he/en), rubrics, judge prompts, results/
├── tests/              unit/, integration/, security/, e2e/, fixtures/
└── docs/               ADRs, runbooks, HMI design
```

---

## 4. משטר "אנליסט במשרה מלאה" — יממת עבודה של הסוכן

| שעה (ישראל) | מצב | מה קורה | מודל |
|---|---|---|---|
| 01:00–06:00 | **BATCH (חלון לילה)** | המחזור המלא (5.1); GPU בבעלות הסוכן | תושב 12B (+ עורך עברי בסוף) |
| 06:00 | מסירה | דוח יומי docx מוכן; ntfy: "דוח מוכן + 3 כותרות" | — |
| 06:00–09:00 | **STANDBY** | מודל קל טעון; מענה מיידי לשאילתות; אין batch | e4b |
| 09:00–23:00 | **POLITE (שעות עבודה)** | RSS poll כל שעתיים (ללא LLM כבד); שאילתות אד-הוק בעדיפות משתמש; חקירה יזומה על פי דרישה — רק אם GPU חיצוני < 25% | e4b; 12B לפי דרישה מפורשת |
| 23:00–01:00 | **PRE-FLIGHT** | בדיקות תקינות, ניקוי, גיבוי pg_dump, טעינת מודל מראש, אימות דיסק/חום | — |
| שבת 01:00–06:00 | **WEEKLY** | דוח שבועי, סריקת כנסים, פטנטים, מכרזים (שלב ג') | 12B |
| 1 בחודש | **MONTHLY** | דוח חודשי, לוח כנסים דו-שנתי, ניקוי retention | 12B |

**הרצה יזומה בכל שעה:** `eo run --now [--scope daily|investigate "<שאלה>"|report]` מה-CLI, מהממשק, או מפקודת ntfy. ההרצה עוברת את אותו שער משאבים; אם אתה עובד על המחשב (GPU חיצוני מעל הסף) — הסוכן שואל בהודעת ntfy "להריץ בכל זאת? (eco / full / דחה ללילה)".

---

## 5. הפייפליין הלילי ולוחות זמנים

### 5.1 סדר ריצה (Asia/Jerusalem, DST-aware; זמנים משוערים)

```
00:55  wake-guard     Task Scheduler מוודא שהמחשב ער; pre-flight עבר
01:00  ingest         RSS/HTML → fetcher → סניטציה → Prompt Guard → items      (~25 דק', CPU בעיקר)
01:25  embed+dedup    EmbeddingGemma → cosine ≥ 0.92 מול 7 ימים → dedup       (~10 דק')
01:35  classify       12B batch: תחום, ממדים, ישויות, גיאוגרפיה, סוג דיווח   (~30 דק' ל-200 פריטים)
02:05  triage         12B: ציון 1-10 + רמה; כיול לפי triage_feedback          (~15 דק')
02:20  deep_search    לכל 🔴 (עד 4 בלילה, 25 דק' כל אחת): ReAct רב-לשוני    (עד 100 דק')
04:00  analyze        12B: summary_he, So-What, events, edges לגרף            (~45 דק')
04:45  report         Markdown → docx RTL → QA סימוכין (חוסם) → (עורך עברי)   (~30 דק')
05:15  export+backup  Obsidian (שלב ב'), pg_dump, אינדקסים                      (~10 דק')
05:25  notify         ntfy: דוח + 🔴 + סיכום חקירות + נקודות פתוחות
05:30–06:00  רזרבה    carry-over של חקירות שלא הושלמו / ניסיון חוזר לשלב שנכשל
06:00  HARD DEADLINE  כל job שעדיין רץ מקבל SIGTERM עדין → נשמר כ-partial → מדווח
```

### 5.2 תקציב זמן (Deadline Budgeting)
- לכל שלב תקציב זמן וגבול עליון; המתזמן מחשב "זמן שנותר עד 06:00" לפני כל שלב ומקצץ: פחות חקירות, פחות שפות, סיכומים קצרים יותר — **לעולם לא מדלג על הדוח**.
- אם ingest נכשל כליל — הדוח יוצא עם הודעה "לא נאספו פריטים חדשים" ולא נעצר.

### 5.3 Carry-over
חקירה שלא הסתיימה נשמרת ב-`jobs` עם state ומתחדשת בלילה הבא (או בהרצה יזומה) מהסבב שבו נעצרה. הדוח מסמן "⏳ חקירה בתהליך".

### 5.4 DST ושעון
כל הקונטיינרים עם `TZ=Asia/Jerusalem`; APScheduler עם `timezone="Asia/Jerusalem"`; זמנים ב-DB ב-`timestamptz` (UTC). בדיקת יחידה למעבר שעון (מרץ/אוקטובר).

---

## 6. הגנה מקריסה — Resource Governor בארבע שכבות

| שכבה | מנגנון | ערכי ברירת מחדל |
|---|---|---|
| **L0 מארח** | .wslconfig (RAM 36 GB, 16 CPU, swap 8 GB); Docker Desktop ללא Resource Saver בלילה | |
| **L1 קונטיינר** | `deploy.resources.limits`: worker 20 GB RAM / 8 CPU; postgres 6 GB; searxng 1 GB; fetcher 2 GB; `pids_limit`; `restart: unless-stopped` | |
| **L2 שער משאבים (לפני כל היסק)** | VRAM פנוי ≥ דרישה + 1.2 GB; GPU util חיצוני < 25% (ביום); RAM פנוי ≥ 8 GB; דיסק ≥ 20 GB; טמפ' GPU < 83°C; אחרת: תור עם backoff 5→10→30→60 שנ' עד 20 דק' → דחייה + לוג + ntfy | |
| **L3 Watchdog** | heartbeat כל 30 שנ' לטבלת `run_log`; ללא heartbeat 5 דק' → kill job → restart שלב; 3 כשלים באותו שלב = circuit breaker (השלב מדולג הלילה, ntfy); OOM ב-Ollama → הקטנת num_ctx ב-50% וניסיון חוזר פעם אחת | |

תוספות:
- **תרמי (מחשב נייד):** מעל 83°C — השהיה 2 דק'; מעל 88°C — עצירת batch והמתנה; מומלץ מעמד קירור ו-power plan "Best performance" רק ב-AC.
- **עדיפות תהליכים:** קונטיינר worker עם `cpu_shares` נמוך; ביום — `nice`-equivalent כדי שלא יפריע לך.
- **דיסק:** watchdog מסרב להתחיל לילה עם < 20 GB; ניקוי אוטומטי של cache ולוגים ישנים; התראה ב-< 40 GB.
- **התאוששות מאתחול:** Docker AutoStart קיים; `jobs` persist מאפשר המשך; בדיקת "מי רץ כשנפלנו" ב-pre-flight.
- **מדידה:** כל החלטת שער ל-`resource_log`; פאנל הסטטוס מציג היסטוריית 24 שעות.

---

## 7. ארכיטקטורת האפליקציה (עדכון תמציתי)

```
[scheduler] ──jobs──► [orchestrator]  (קונטקסט נקי, כלים)
                          │
        ┌─────────────────┼──────────────────┐
        ▼                 ▼                  ▼
   [fetcher]          [worker]           [notify]
   egress only        internal only      ntfy
   RSS/HTML/SearXNG   Prompt Guard →     
   sanitize → PG      12B (ללא כלים) →  
                      JSON לפי סכמה → PG
                          │
                          ▼
             [PostgreSQL + AGE + pgvector]
             items/entities/events/contracts/sources/conferences
             jobs/run_log/resource_log/model_registry/security_log
             triage_feedback/lessons/search_playbook/investigation_log
                          │
                          ▼
                  [report] docx RTL + QA ──► output/
                  [web-ui] FastAPI+React (127.0.0.1)
```
עקרונות שנוספו: תור `jobs` persist; כל קריאת LLM דרך `llm/ollama_client.py` (lint אוסר import של httpx לכתובת Ollama במקום אחר); כל פלט מודל מאומת Pydantic; provenance בכל רשומה.

---

## 8. ממשק אדם-מכונה מתקדם (HMI)

### 8.1 תפיסה — שלוש חלופות (D1)

| חלופה | תיאור | יתרונות | חסרונות |
|---|---|---|---|
| **A. קונסולת מבצעים (dashboard-first)** | מסך ראשי = לוח מצב: פיד triage, מדדים, סטטוס משאבים; צ'אט בפאנל צד | סקירה מהירה; מתאים לבוקר; שקיפות מערכת | פחות מזמין לחקירה חופשית |
| **B. אנליסט-שיח (chat-first)** | מסך ראשי = שיחה עם הסוכן; הדוח, הפיד והגרף נפתחים כ"כרטיסים" בתוך השיחה | טבעי; שאלות פתוחות; פחות מסכים | קשה לסרוק 50 פריטים; סטטוס נסתר |
| **C. היברידי "חדר מצב + עמית"** (מומלץ) | מסך ראשי = לוח מצב עם פיד; פאנל שיחה קבוע מימין ("שאל את האנליסט"); כל פריט/ישות/גרף ניתן לגרירה לתוך השיחה כהקשר | משלב סריקה מהירה עם חקירה; הקשר מפורש | דורש עיצוב קפדני של צפיפות |

### 8.2 מסכים (לחלופה C)

1. **הבוקר** — תקציר מנהלים של הלילה, 3 כותרות, "מה דורש הכרעה" (FR-10), כפתור "פתח דוח docx".
2. **פיד Triage** — רשימה צפופה עם ציון, רמה, תחום, מקורות; פעולות מקלדת (J/K, 1-4 לשינוי רמה, X ללא רלוונטי) → `triage_feedback`; "למה הציון?" מציג נימוק המודל.
3. **חוקר ישויות וגרף** — Cytoscape: קודקודים לפי סוג, קשתות עם provenance (hover → הפריט המקורי); שאילתות Cypher מוכנות ("ספקים של מתחרי X"); ציר זמן ישות.
4. **חקירות עומק** — לוג חי של ReAct: סבב, שפה, שאילתה, מה נמצא/לא; כפתור "המשך חקירה" / "עצור".
5. **שאל את האנליסט** — צ'אט RAG עם ציטוטים [n] לחיצים; "הצג את המקור" פותח את הטקסט המסונן; אפשר לצרף פריטים/ישויות כהקשר.
6. **לוח כנסים** (שלב ג') — ציר זמן 24 חודשים, מסננים, ייצוא iCal.
7. **הבהרות ומשוב** — תיבת דואר: שאלות סגורות של הסוכן, שאלון יומי מתחלף (FR-11), היסטוריית "מה למדתי ממך".
8. **פאנל סטטוס משאבים** (קבוע, תחתית/צד) — VRAM/GPU/טמפ'/RAM/דיסק, מודל טעון, תור jobs, השלב הנוכחי, החלטות שער אחרונות, שירותים (PG/SearXNG/Ollama) — עדכון WebSocket כל 2 שנ'.
9. **הגדרות** — עריכת YAML (watchlist, מקורות, טקסונומיה, חלון לילה) עם ולידציה; מצב eco/full.

### 8.3 מערכת עיצוב
- RTL מלא (`dir="rtl"`), גופנים: Heebo/Assistant לעברית, Inter למונחים לטיניים; מספרים וקודים LTR מבודדים (`bdi`).
- ערכת צבעים כהה כברירת מחדל (חדר מצב), בהירה לקריאת דוחות; רמות triage בצבעים נגישים (לא רק אדום/כתום).
- צפיפות מידע גבוהה עם היררכיה טיפוגרפית ברורה; ללא אנימציות מיותרות.
- נגישות: ניווט מקלדת מלא, ARIA, ניגודיות AA.
- **מובייל:** ntfy עם כפתורי פעולה (Action buttons): "אשר / דחה / eco" — כדי לענות להבהרות מהנייד בשניות; ה-UI עצמו responsive לקריאה.

### 8.4 תהליך (FR-13)
1. מסמך חלופות (טבלה 8.1 מורחבת) → **הכרעתך**.
2. אב-טיפוס אינטראקטיבי ב-Claude Design לחלופה הנבחרת: כל 9 המסכים, RTL, קליקבילי, עם נתוני דמה מהתחום.
3. **אישורך** → האב-טיפוס הוא מפרט העיצוב; מימוש ב-React בשלב 4.
4. בדיקות שמישות: 3 מפגשי "בוקר אמיתי" איתך במהלך הפיילוט; תיקונים.

---

## 9. אסטרטגיית בדיקות

### 9.1 פירמידה

| רמה | כלי | מה נבדק | מתי רץ |
|---|---|---|---|
| **יחידה** | pytest, hypothesis | סניטציה, פרסור RSS, סכמות Pydantic, חישובי שער משאבים (nvidia-smi מדומה), DST, QA סימוכין, בניית docx | כל commit (pre-commit + CI) |
| **אינטגרציה** | testcontainers (PG+AGE+pgvector) | DDL, trigger entities↔גרף, Cypher, pgvector dedup, jobs persist/resume | כל PR |
| **אבטחה** | pytest + `tests/fixtures/injection_samples/` (≥ 60 דגימות: ישיר, מוסתר CSS, base64, unicode, רב-לשוני, דרך RSS title, דרך PDF) | 100% חסימה; אפס הפעלת כלים; הסגר + security_log + blocklist אחרי 2 | כל PR + לילי |
| **הערכת LLM (evals)** | `evals/` — סט זהב: 150 פריטים מתויגים ידנית (תחום, רמה, ישויות); 30 סיכומים בעברית עם rubric; 10 אירועים ידועים לחקירה + 5 פיקטיביים | דיוק סיווג ≥ 85%, triage ±1 ב-≥ 80%, עברית ≥ 4/5 (שופט: 12B בפרומפט rubric + דגימה ידנית שלך), פיקטיבי → not_found 100% | לילי (על GPU, אחרי ה-batch) + לפני החלפת מודל |
| **דוח** | python-docx round-trip + LibreOffice headless → PDF → בדיקת RTL/כותרות/TOC; אפס [n] לא ממופה | docx נפתח ב-Word ללא שגיאות | כל PR |
| **ביצועים** | 200 פריטים סינתטיים | classify+triage < 45 דק'; אד-הוק ≤ 30 שנ' | שבועי |
| **עמידות (chaos)** | הריגת ollama/postgres באמצע שלב; ניתוק רשת ל-fetcher; מילוי דיסק מדומה; VRAM תפוס מדומה | התאוששות; carry-over; אפס נתונים אבודים | שבועי |
| **UI** | Playwright (RTL screenshots, מקלדת), vitest | מסכים, WebSocket סטטוס, משוב נכתב ל-DB | כל PR |
| **קבלה (E2E)** | "לילה יבש": מחזור מלא על fixture של 50 פריטים בסביבת בדיקה | דוח + ntfy + לוגים תקינים | לפני כל שלב |
| **אבטחת רשת** | pktmon/Wireshark 24 שעות | תעבורה יוצאת רק מ-fetcher/searxng ליעדים צפויים | פעם בשלב + אחרי עדכון Ollama/Docker |

### 9.2 כללים
- אין `skip`/`xfail` ללא ticket; fixture הזרקה חדש לכל אירוע אמיתי שנתפס בפיילוט.
- CI: GitHub Actions ל-unit/integration/security (ללא GPU); evals ו-performance רצים מקומית בלילה ומדווחים ל-`run_log` ולפאנל.
- כל שלב בתוכנית נסגר רק עם "Definition of Done" מדיד (פרק 10).

---

## 10. תוכנית העבודה המפורטת

יחידת הערכה: **סשן פיתוח** ≈ חצי יום עבודה של Claude Code איתך. הערכות כוללות בדיקות.

### Phase 0 — הכנת סביבה ו-bake-off מודלים (3–4 סשנים)

| # | משימה | תוצר | קבלה |
|---|---|---|---|
| P0.1 | ניקוי מודלים לא-מערביים (באישורך), יעד ≥ 200 GB פנויים; .wslconfig; Task Scheduler wake | דיסק ומארח מוכנים | `df` ≥ 200 GB; WSL מוגבל |
| P0.2 | `models.yaml` + `models.lock` + סקריפט אימות digest; הורדה: multilingual-e5-large-instruct, Prompt Guard 2 (86M), Granite Guardian 4.1 (2B/3B), gemma4:26b, ministral-3 14B (למתמודד); הגדרת `OLLAMA_NO_CLOUD=1` + `OLLAMA_HOST=127.0.0.1` + כלל firewall | רישום מודלים | כל מודל עם digest; `ollama list` ללא מודל סיני |
| P0.3 | **Bake-off**: gemma4:12b מול gemma4:26b (MoE, offload) מול DictaLM-3.0-12B מול gpt-oss:20b (offload) מול ministral-3-14B על 30 פריטים: סיווג JSON, סיכום עברי, So-What, ReAct עם 3 כלים מדומים; מדידת tok/s, VRAM שיא, טמפ' GPU, איכות עברית (rubric + דגימה שלך) | `docs/adr/001-models.md` | הכרעה מתועדת: מודל תושב, עורך עברי (כן/לא), מודל חקירה (כן/לא) |
| P0.4 | בדיקת Ollama בקונטיינר עם bind mount למודלים; השוואה לנייטיבי | ADR-002 | tok/s בקונטיינר ≥ 90% מנייטיבי |
| P0.5 | שלד רפו, uv, ruff, mypy, pre-commit, CI בסיסי | רפו עובד | `pytest` ירוק על שלד |

### Phase 1 — תשתית (4–5 סשנים)

| # | משימה | קבלה |
|---|---|---|
| P1.1 | אימג' postgres+AGE+pgvector (גרסה נתמכת), compose עם שתי רשתות, מכסות, healthchecks, TZ | `up` מרים הכל; worker ללא אינטרנט (בדיקה: `curl` נכשל) |
| P1.2 | Alembic migrations: כל הטבלאות כולל jobs/run_log/resource_log/model_registry/security_log; ממד וקטור מ-config | testcontainers ירוק |
| P1.3 | graph_init + trigger סנכרון + `memory/graph.py` עם 6 שאילתות מוכנות | Cypher "שותפי מתחרי X" עובר על seed |
| P1.4 | `llm/ollama_client.py`: chat/tools/json-schema, retries, Pydantic; lint שאוסר עקיפה | sanity לכל היכולות |
| P1.5 | `resources/`: gate, gpu, thermal, disk; resource_log; סימולטור nvidia-smi לבדיקות | קריאה ללא VRAM → תור → דחייה מתועדת |
| P1.6 | scheduler + jobs persist + deadline budgeting + night-window + polite mode | "לילה יבש" ריק רץ 01:00→06:00 מדומה (זמן מואץ) |
| P1.7 | install.ps1/.sh + `eo` CLI (`run`, `status`, `investigate`, `report`) | התקנה ממכונה נקייה (VM) ללא הורדות < 20 דק' |

### Phase 2 — איסוף, אבטחה, סיווג (5–6 סשנים)

| # | משימה | קבלה |
|---|---|---|
| P2.1 | fetcher: RSS + HTML + סניטציה (trafilatura, hidden-text, base64, unicode) + sources.yaml עם 25 מקורות ראשוניים (כולל מקורות יבשה/ים) | 25 מקורות נמשכים; כשל אחד לא מפיל |
| P2.2 | Prompt Guard 2 (86M) על CPU + היוריסטיקות + Granite Guardian 4.1 לחשודים; הסגר; blocklist; security_log | 100% מ-60 דגימות נחסמות; false-positive < 3% על 200 פריטים לגיטימיים |
| P2.3 | embeddings + dedup חוצה-שפות (fixture he/en/ru של אותו סיפור) | dedup נכון על fixture |
| P2.4 | classify + triage batch עם סכמות; טקסונומיה כולל תחומים חדשים; כיול לפי triage_feedback | סט זהב: סיווג ≥ 85%, triage ±1 ≥ 80%; 200 פריטים < 45 דק' |
| P2.5 | evals harness + סט זהב ראשון (אתה מתייג 150 פריטים בעזרת כלי תיוג מהיר ב-CLI) | דוח evals אוטומטי |

### Phase 3 — חקירה, ניתוח, דוח, התראות (6–7 סשנים)

| # | משימה | קבלה |
|---|---|---|
| P3.1 | SearXNG בקונטיינר (ללא פורט חיצוני), client, מנועים נבחרים, rate limits | חיפוש עובר דרך fetcher בלבד |
| P3.2 | Deep Search ReAct: 4 סבבי התמדה, תקציב שאילתות/עמודים/זמן, investigation_log, search_playbook | אירוע ידוע → ≥ 3 מקורות; פיקטיבי → not_found עם לוג מלא |
| P3.3 | רב-לשוני: תרגום שאילתות עם התאמת מונחים; מיפוי שפה↔מקור; נרמול לעברית | fixture בשלוש שפות |
| P3.4 | analyze: summary_he, So-What, events, edges עם provenance | אירוע עסקי → events + קשת בגרף |
| P3.5 | דוח יומי: Markdown → docx RTL (תבנית, TOC, טבלת אירועים, נספח מקורות) + QA סימוכין חוסם + עורך עברי אופציונלי | docx תקין ב-Word; אפס טענות ללא [n] |
| P3.6 | ntfy: דוח, 🔴, אבטחה, כשלים, הבהרות עם כפתורי פעולה | הודעות מגיעות לנייד |
| P3.7 | watchdog + circuit breaker + chaos tests | כל תרחישי chaos מתאוששים |
| P3.8 | **לילה אמיתי ראשון** (01:00–06:00) עם ניטור | דוח בשעה 06:00 |

### Phase 4 — HMI (5–6 סשנים, מתחיל במקביל ל-Phase 2)

| # | משימה | קבלה |
|---|---|---|
| P4.1 | מסמך חלופות D1 + הכרעתך | חלופה נבחרה |
| P4.2 | אב-טיפוס Claude Design: 9 מסכים RTL קליקביליים | אישורך |
| P4.3 | FastAPI backend: REST + WebSocket סטטוס + auth מקומי | OpenAPI + בדיקות |
| P4.4 | React: Shell, הבוקר, פיד triage עם מקלדת, פאנל סטטוס | Playwright ירוק |
| P4.5 | חוקר ישויות/גרף, חקירות עומק, "שאל את האנליסט" עם ציטוטים | שאילתה ≤ 30 שנ' |
| P4.6 | הבהרות + משוב + הגדרות YAML | משוב נכתב ל-triage_feedback/lessons |
| P4.7 | 3 מפגשי שמישות "בוקר אמיתי" | תיקונים מיושמים |

### Phase 5 — פיילוט וכיול (7 לילות + 2 סשנים)

| # | משימה | קבלה |
|---|---|---|
| P5.1 | 7 לילות רצופים ללא התערבות; מדידת KPI (רעש, דיוק, זמינות, זמן GPU, טמפ' מקס) | ≥ 95% מחזורים הושלמו; אפס OOM; אפס קריסת מחשב |
| P5.2 | recall ידני: 10 אירועים מהעיתונות בשבוע → כמה נתפסו | ≥ 8/10 |
| P5.3 | כיול triage ממשוב; עדכון watchlist; ADR מסכם | דוח פיילוט |
| P5.4 | שחזור ממכונה נקייה: install + dump | < 30 דק' ללא הורדות |

### שלבים ב'–ד' (לאחר אישור שלב א')
- **ב':** ייצוא Obsidian, שער הבהרות מלא, שאלון משוב מלא, סיכום מטא שבועי.
- **ג':** מכרזים (SAM.gov/TED), פטנטים, לוח כנסים מתגלגל + iCal, פרופילי ישויות, דוחות שבועי/חודשי, ניתוח מגמות.
- **ד':** הרחבת watchlist אוטומטית, זיהוי אנומליות, כיול מתמשך, קריאת PDF/תמונות עם יכולת vision.

### אבני דרך

| אבן דרך | תלות | סימן |
|---|---|---|
| M0: סביבה מוכנה + ADR מודלים | Phase 0 | bake-off מתועד |
| M1: תשתית עולה, שער משאבים עובד | Phase 1 | לילה יבש ריק |
| M2: פריטים נכנסים ומסווגים בבטחה | Phase 2 | evals ≥ יעד |
| M3: דוח לילי ראשון ב-06:00 | Phase 3 | docx + ntfy |
| M4: ממשק מאושר ופועל | Phase 4 | Playwright + שמישות |
| M5: שלב א' "גמור" | Phase 5 | 7 לילות |

---

## 11. מדיניות דלגציה למודלים זולים (תהליך הפיתוח)

בהתאם לבקשתך, עבודת הפיתוח תפוצל לפי רמת הקושי, כדי לחסוך בעלות Claude:

| סוג משימה | מבצע | דוגמאות |
|---|---|---|
| מחקר עובדתי, אימות גרסאות, איסוף קישורים | סוכן **haiku** ברקע | אימות המודלים (בוצע היום), דוקומנטציית Ollama/AGE |
| boilerplate, בדיקות יחידה, fixtures, docstrings, המרות פורמט | סוכן **haiku** / **sonnet** | 60 דגימות הזרקה, סכמות Pydantic, migrations, seed |
| מודולים סטנדרטיים לפי מפרט ברור | סוכן **sonnet** | fetcher, ntfy, CLI, React components |
| ארכיטקטורה, שער משאבים, ReAct, אבטחה, docx RTL, פרומפטים בעברית, סקירת קוד סופית | **אני (Fable)** | |
| ניתוח קורפוס גדול (סקירת רפו שלם, סיכום 100 מאמרים למקורות) | ערוץ Gemini/Antigravity (`agy`) | סקירה שנייה של diff, מיפוי מקורות RSS |
| **סקירה נגדית** לכל מודול ליבה | סוכן sonnet נפרד מהכותב | לפני merge |

כללים: כל פלט של סוכן זול עובר `ruff`/`pytest` לפני שאני קורא אותו; אני קורא רק כשלים; קוד אבטחה ושער משאבים לא נכתבים על ידי מודל זול.

---

## 12. סיכונים

| סיכון | הסתברות | השפעה | מענה |
|---|---|---|---|
| Gemma 4 12B אינו כותב עברית ברמה מספקת | בינונית | גבוהה | bake-off ב-Phase 0; DictaLM כעורך; שני מודלים = החלפה אחת בלילה בלבד |
| Apache AGE מפגר אחרי גרסאות PG (כיום 1.6.0 תומך ב-PG17) | נמוכה | בינונית | הצמדה ל-PG17; חלופת nodes/edges מוכנה ב-`memory/graph.py` |
| Ollama בקונטיינר איטי / VRAM נגיש נמוך ב-WSL2 | בינונית (דיווחים סותרים) | בינונית | ברירת מחדל: Ollama נייטיבי + firewall; P0.4 מודד |
| חום מחשב נייד ב-5 שעות batch | בינונית | גבוהה (קריסה/בלאי) | שער תרמי; מעמד קירור; מדידה בפיילוט |
| דיסק מתמלא | גבוהה (123 GB) | גבוהה | ניקוי מיידי; watchdog; retention |
| SearXNG נחסם ע"י מנועים | בינונית | בינונית | מספר מנועים, rate limit, cache; RSS כמקור עיקרי |
| הזרקה עוקפת את השומר | נמוכה | גבוהה | worker ללא כלים ורשת — גם הצלחה בהזרקה לא מאפשרת פעולה; fixture חדש לכל אירוע |
| היקף הפרויקט מול קצב הסשנים | גבוהה | בינונית | אבני דרך מדידות; שלב א' לפני הכל; UI במקביל בלבד |

---

## 13. הכרעות שהתקבלו (4.9.2026)

| # | נושא | הכרעה | השלכה בתוכנית |
|---|---|---|---|
| 1 | ניקוי מודלים לא-מערביים | **אושר ובוצע** — הוסרו 7 מודלים (Qwen ×4, DeepSeek, bge-m3); תיקיית Ollama ירדה מ-186 GB ל-80 GB; דיסק פנוי 227 GB | P0.1 הושלם חלקית; יעד ≥ 200 GB הושג |
| 2 | DictaLM 3.0 כעורך לשוני עברי | **אושר לבחינה** ב-bake-off | P0.3 |
| 3 | ntfy | **self-hosted** בקונטיינר (פרטי יותר). גישה מהנייד מחוץ לבית דרך Tailscale (חינמי, ללא פתיחת פורטים) | P1.1 מוסיף שירות `ntfy`; P3.6; הוראת התקנת Tailscale ב-install |
| 4 | חלופת ממשק | **היברידי (C)** — "חדר מצב + עמית". ניתן לשנות אם יתגלה במהלך העבודה שחלופה אחרת עדיפה. **הדוח תמיד קיים גם כקובץ docx** לקריאה עצמאית, בלי תלות בממשק | P4.1 מקוצר: מסמך D1 הופך לאישור; P4.2 מתחיל מיד. `output/` נגיש גם ללא UI |
| 5 | תקרת חקירות עומק ללילה | **4** כברירת מחדל; פרמטר `deep_search.max_per_night` ב-config; נבחן הרחבה (6–8) אחרי הפיילוט לפי זמן GPU בפועל | P1.6, P3.2; מדד "זמן חקירה ממוצע" ב-P5.1 |
| 6 | כונן D: | **לא קיים** — המודלים נשארים ב-C: | watchdog דיסק עם סף 40 GB התראה / 20 GB עצירה; retention קשיח |

### 13.1 מצב מוכנות לתחילת ריצה (Phase 0)

| פריט | מצב |
|---|---|
| חומרה מאומתת (GPU 12 GB, RAM 64 GB, GPU בקונטיינר) | ✅ |
| דיסק ≥ 200 GB | ✅ (227 GB) |
| מודלים מערביים בסיסיים מותקנים (gemma4 12b/e4b/e2b, gpt-oss, nemotron-3.5, DictaLM 12B/24B) | ✅ |
| מודלים להורדה ב-P0.2 (multilingual-e5, Prompt Guard 2, Granite Guardian 4.1, gemma4:26b, ministral-3-14B) | ⏳ ~25 GB |
| `.wslconfig` | ⏳ P0.1 |
| Ollama: `OLLAMA_NO_CLOUD`, `OLLAMA_HOST`, firewall | ⏳ P0.2 |
| Task Scheduler wake 00:55 | ⏳ P0.1 |
| שלד רפו + כלי איכות | ⏳ P0.5 |
| Bake-off + ADR מודלים | ⏳ P0.3 |
| הכרעות משתמש | ✅ כולן התקבלו |
