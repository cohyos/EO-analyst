# ניקוי ארכיון הדוחות — 13.09.2026

נבדקו 200 רשומות דוח. 149 הוצאו מהרשימות הפעילות והועברו להסגר:

- 139 גרסאות ישנות הפנו לאותם קבצים שנכתבו מחדש עבור גרסאות מאוחרות. הנתיבים נורמלו גם מהקידומת הישנה `/app/` לנתיבי Windows.
- 4 דוחות נכשלו בבדיקת האיכות הקיימת, כולל דוח עם סימון זיהום מתוכן בדיקה.
- 6 דוחות נוספים הכילו קישורי `#src-N` ללא יעד בנספח המקורות: 158, 161, 188, 194, 216, 218.

51 דוחות נשארו פעילים. לכל אחד נשמר עותק עצמאי של Markdown, HTML ו־Word תחת `output/reports/versions`. נבדקו קיום ותוכן לא ריק של הקבצים, תקינות ארכיון ה־Word, והפניות פנימיות למקורות. לא נמצאו מזהי פריטי מקור חסרים במערכי `items_included`, הפניות כפילות עצמיות/חסרות ב־items, או פריטים שסומנו גם חסומים וגם כתוכן נקי.

זהו ניקוי תקינות והפניות, ולא אימות עובדתי מלא של הטענות בדוחות. מעבר QA קודם אינו הוכחה לאמיתות כל טענה.

## גיבוי ושחזור

לא נמחקו רשומות דוח, פריטי מקור, משובים או קשרי מסד נתונים. ברשומות שבודדו נוספה סיבת הסגר ב־`qa_report.archive`, והנתיבים הפעילים אופסו. קישורים ישנים לדוח מציגים הודעת הסגר; הורדות ומקורות של דוח מבודד אינם מוגשים. נשמרו גיבויים של רשומות הדוחות ושל הקבצים, כולל SHA-256 ואימות העתקה:

- `runtime/backups/report-quarantine-20260913T184450058003Z` — מצב לפני ניקוי 143 הרשומות הראשונות.
- `runtime/backups/report-quarantine-20260913T184645015584Z` — מצב לפני בידוד 6 הדוחות עם הפניות שבורות.
- `runtime/backups/data-cleanup-20260913/verification-after.json` — תוצאת הבדיקה הסופית.

לכל גיבוי `reports.json`, `manifest.json`, `applied.json` ותיקיית קבצים. שחזור ניקוי צריך לעדכן רק את `path_docx`, `path_md`, `path_html` ו־`qa_report` של הרשומות המושפעות, אחרי בדיקה שלא השתנו מאז; אין להחליף את כל המסד או לפגוע בדוחות חדשים. הגיבוי משמר את הקבצים שהיו קיימים בעת הבדיקה — אינו משחזר תוכן היסטורי שכבר נדרס לפני הבדיקה.

## מניעת הישנות ואימות

שבעת מחוללי הדוחות כותבים כעת כל גרסה בתיקייה ייחודית. רשימות הדוחות מסננות הסגר במסד הנתונים לפני הגבלת מספר התוצאות. תוקן גם קיבוץ קווי מוצר וסקירות מוצר לפי זהות המוצר, במקום איחוד מוצרים שונים באותו חלון תאריכים.

הכלי `scripts/quarantine_report_artifacts.py` מבצע בדיקה בלבד כברירת מחדל; `--apply` שומר גיבויים ומבצע עדכון בעסקה עם בדיקת שינויים מקבילים. ריצה חוזרת ללא ממצאים אינה משנה נתונים.

70 בדיקות ממוקדות עברו; לאחר חיזוק הסתרת קבצים מבודדים הורצו שוב 40 בדיקות רלוונטיות וכולן עברו. Ruff עבר. ניסיון להריץ גם בדיקות מחוללי דוחות רחבות הופסק בשל המתנות לתלויות שלא בודדו במתקני הבדיקה; אין לטעון שכל החבילה הרחבה הושלמה.

שרת הממשק בלבד אותחל והדפדפן נטען מחדש. ריצת הלילה 264 נשארה פעילה ללא אתחול worker. מחוללים שכבר נטענו בתהליך יקבלו את תיקון שמות הקבצים רק בטעינה הבאה/אתחול עתידי; עותקי 51 הדוחות הקיימים כבר מוגנים גם מפני worker ישן. המודלים המקומיים נשארו מושהים ובדיקת Ollama לא הציגה מודלים טעונים.
