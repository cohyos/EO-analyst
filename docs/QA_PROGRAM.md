# תוכנית בקרת איכות שיטתית — EO-Analyst (לולאת בדיקה–תיקון עד התייצבות)

**הוראת המשתמש (2026-09-06 02:00):** מעבר לילי על המערכת והתוכן: בדיקת עומק של תוכן השדות, הקשר ונכונות, תקינות השדות ואופן ההפעלה, האם ההיסקים נראים כהיסק של אנליסט מהתחום, תקפות הנתונים באתרים שהמערכת מפנה אליהם; כל תקלה → תיקון מאומת → חזרה על הבדיקות עד שתהליך האיכות מתייצב.

## 1. עקרונות
- **שכבות בלתי תלויות, מבצעים נפרדים:** כל שכבה נבדקת על ידי סוכן QA שאינו הסוכן שכתב את הקוד. הממצאים נכתבים לקובץ `docs/qa/findings_<שכבה>_r<סבב>.md` בפורמט אחיד. התיקון נעשה על ידי סוכן אחר; האימות מחדש על ידי סוכן QA.
- **פורמט ממצא:** `| # | חומרה (P1 שובר/מטעה, P2 פוגע בערך, P3 קוסמטי) | היכן (מסך/שדה/קובץ/URL) | תיאור | הוכחה (פלט/צילום/שאילתה) | שורש משוער | סטטוס |`.
- **קריטריון התייצבות:** סבב שבו 0 ממצאי P1, 0 ממצאי P2 חדשים, ומספר P3 הפתוחים לא עולה — ואז סבב אימות נוסף שמאשר. עד אז חוזרים.
- **ראיות בלבד:** "עבר" נאמר רק עם פלט מצורף (פקודה + תוצאה, צילום מסך, שורת DB).

## 2. השכבות

| # | שכבה | מה נבדק | כלים/שיטה | סף מעבר |
|---|---|---|---|---|
| Q1 | קוד | ruff, mypy, pytest unit (מלא), בדיקות אינטגרציה חיות מול המערכת המקומית, סריקת TODO/skip/stub, קוד מת, pip-audit לחולשות בתלויות, npm audit | סוכן קוד | 0 כשלים; 0 חולשות high; אין stubs |
| Q2 | אבטחה | הזרקת פרומפט (סט התקפות בעברית/אנגלית/מוסתרות) מול שומרי L1/L2 בזמן אמת; SSRF דרך fetch/read/MCP; הורדת קבצים (path traversal); settings API ללא token; סודות בלוגים/DB/דוחות; מסגור DATA בכל פלט כלי; מצב מקומי לא שולח כלום החוצה (בדיקת קוד + ניטור קריאות); סקירה עצמאית של Codex על ה-diff של 24 השעות | סוכן אבטחה + Codex | 0 P1 |
| Q3 | תוכן והיסק אנליסטי | מדגם 40 פריטים (כל הרמות): סיכום/so-what/עובדות מפתח מול טקסט המקור (הורדה חיה); דומיין/תת-דומיין; רמת מיון מול חוקי המיון; ישויות; אירועים (סוג/תאריך/סכום/צדדים) מול המקור; מכרזים (רלוונטיות, תאריכים, סטטוס); תחזיות (הגיון הסיבתיות); תשובות חקירות מול המקורות; הדוחות היומי/שבועי: קוהרנטיות, כפילויות, מיפוי [n] נכון, עברית מקצועית (מונחים: כטב"ם, מטע"ד, פוד כיוון), "אז מה" ברמת אנליסט | סוכן תוכן עם רובריקה 1–5 לכל שדה | ממוצע ≥ 4; 0 סיכומים שסותרים את המקור |
| Q4 | תקפות קישורים ונתונים חיצוניים | כל URL בפריטים (7 ימים), מכרזים, כנסים, נספחי דוחות: קוד HTTP, הפניות, דומיין חונה, התאמת כותרת העמוד לכותרת הפריט; תאריכי כנסים מול האתר הרשמי; דדליינים של מכרזים מול עמוד ההודעה | סוכן קישורים (httpx + קריאת עמוד) | 0 קישורים שבורים בדוחות; כנסים 100% מאומתים |
| Q5 | UI/UX ודרישות ממשק | Playwright מלא (דסקטופ+מובייל); מעבר ידני בדפדפן עם צילומי מסך: כל מסך, כרטיס, טאב, כפתור, מעבר, מצב ריק/טעינה/שגיאה, מקלדת, a11y (axe), RTL/LTR בעברית ובאנגלית, שתי ערכות נושא; טקסטים (שגיאות כתיב, ערבוב שפות); משוב על פעולות (toasts, מצבי ריצה); אימות שכל פריט ב-REVIEW_2026-09-05 שסומן "תוקן" אכן תוקן במסך | סוכן UI עם דפדפן | 0 P1/P2; e2e ירוק |
| Q6 | תפעול והפעלה | `eo native start/stop/status`, סופרווייזר: התאוששות מקריסה (kill uvicorn → חזרה), רוטציית לוגים, NIGHT_CHECKLIST, תרגיל גיבוי/שחזור ל-DB זמני, gate משאבים, רשימת משימות מתוזמנות, מסירת ntfy, התנהגות בזמן RAM נמוך | סוכן תפעול | כל סעיף בצ'קליסט ירוק |
| Q7 | עקיבות דרישות | מטריצת FR מהמפרט v1.4 ותוכנית v2 מול המימוש: קיים/חלקי/חסר עם ראיה (קובץ/מסך) | סוכן דרישות | אין FR "חסר" בלי החלטה מתועדת |

## 3. הלולאה
1. **סבב r1 (לילה 2026-09-06):** Q1–Q7 במקביל → קבצי ממצאים.
2. **טריאז' (Fable):** מיזוג לטבלה אחת `docs/qa/TRIAGE_r1.md`, חומרה, שיוך לחבילת תיקון, החלטה "לא מתקנים" מתועדת.
3. **תיקון:** סוכני תיקון לפי חבילות, בעלות קבצים מפורשת, בדיקות יחידה לכל תיקון, קומיט לכל חבילה.
4. **אימות:** סוכן QA (לא המתקן) חוזר על הבדיקות של השכבות שנגעו בהן; ממצא נסגר רק עם ראיה.
5. **סבב r2, r3…** עד קריטריון ההתייצבות. כל סבב מקבל שורה ב-`docs/qa/STATUS.md` (ממצאים חדשים/סגורים/פתוחים לפי חומרה).
6. **חתימה:** e2e מלא ירוק, unit+integration ירוקים, דוח לילי אחד שנבדק ידנית מקצה לקצה, ו-`STATUS.md` מעודכן.

## 4. ממצאים פתוחים שנכנסים ישירות לסבב r1 (מהמשתמש, 02:00)
- **F23** הערות שוליים [n] בדוחות: ב-docx הן טקסט בלבד (0 הערות שוליים, 5 קישורים בלבד), ב-HTML `span.cite` בלי קישור; נדרש: ב-docx הערות שוליים אמיתיות או קישורים פנימיים לנספח + קישור חוץ למקור; ב-HTML עוגן לנספח + tooltip + קישור חוץ; באפליקציה — אימות שהלחיצה מנווטת.
- **F24** רשימת מכרזים מיושנת ולא רלוונטית: 21 שורות, מהן 8 סגורות (2015–2025) ו-13 "לא ידוע" ללא תאריכים, חלקן לא רלוונטיות (Green Tech Projects, Scribd PDF); נדרש: הסורק לא מכניס הודעות סגורות/ישנות/לא רלוונטיות כלל (relevance ≥ 6, notice_type תקף, דדליין עתידי או פרסום ≤ 90 יום), מסך המכרזים מציג כברירת מחדל פתוחים + חדשים בלבד עם מסנן "הצג סגורים/לא ידוע", טיהור השורות הקיימות.
