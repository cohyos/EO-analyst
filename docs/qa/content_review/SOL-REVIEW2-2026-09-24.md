## Verdict

Items reviewed: **29**.  
Fixed: **11**; Still partial: **16**.  
Not fixed: **0**; Fix is wrong: **2**.  
Safe to deploy: **No**. This was a read-only code review; I did not rerun the user-reported test suites.  
Top risk: the pinned-IP transport can reuse TLS across different hostnames on one IP, and the new notification marker can permanently suppress a failed send.

## Status table

| ID | Status | Evidence (file:line) | Remaining gap | Follow-up |
|---|---|---|---|---|
| F01 | Still partial | `agent/eoa/fetch/html.py:263-281`; `agent/eoa/fetch/service.py:841-843` | Dialling is pinned, but the shared pool can reuse a TLS connection verified for another hostname on the same IP. | Preserve hostname-specific pooling while dialling the validated IP; test two hosts on one IP. |
| F02 | Fixed | `agent/eoa/orchestrator/jobs.py:391-393`; `agent/eoa/search/deep_search.py:2657-2662` | — | — |
| F03 | Still partial | `agent/eoa/api/services.py:3317-3329`; `agent/eoa/memory/relational.py:1440-1445`; `agent/eoa/orchestrator/jobs.py:931-953` | Deferred cancellation is fixed; replay can now lose a notification because “sent” is committed before delivery. | Fix N03’s delivery protocol. |
| F05 | Still partial | `agent/eoa/fetch/service.py:687-716` | `last_ok_at` is used, but a never-successful source still gets only the default three-day lookback after a longer outage. | Define bounded first-success backfill. |
| F06 | Still partial — needs confirmation | `agent/eoa/api/auth.py:393-408,464-489,573-586` | A tailnet ASGI peer is treated as remote, with login available; that code path does **not** itself lock out the iPhone over HTTPS Serve. Actual Serve forwarding headers were not verified. | Check genuine and spoofed requests through the installed Serve mapping. |
| F09 | Still partial | `agent/eoa/fetch/service.py:323-353`; `agent/eoa/memory/relational.py:232-241,334-385` | Improved content resets stages and clears `dedup_of`, but old score, level, and summaries remain visible until reanalysis. | Clear or hide dependent outputs on accepted refresh. |
| F11 | Fixed | `agent/eoa/fetch/service.py:354-368,395-404` | — | — |
| F20 | Still partial | `agent/eoa/pipeline/story_clustering.py:264-280`; `agent/eoa/memory/relational.py:1968-1970,1996-1998` | Current-member updates commit before historical root remapping. A crash between them can leave the historical split unrecoverable on retry. | Commit both updates together. |
| F22 | Still partial | `agent/eoa/orchestrator/jobs.py:579,641-655` | Weekly runs the daily pipeline after three hours even if daily is still running; it treats a deferred daily as terminal. | Wait for an explicit terminal daily state. |
| F24 | Fixed | `agent/eoa/orchestrator/jobs.py:456-469`; `agent/eoa/orchestrator/execution.py:18-24` | — | — |
| F26 | Still partial | `agent/eoa/llm/providers/api.py:218-235,456-473` | Usage *containers* are checked, but malformed token values or content still raise outside the provider error boundary and bypass fallback. | Validate and construct the full result inside that boundary. |
| F28 | Fixed | `agent/eoa/api/routes/security_review.py:162-171`; `agent/eoa/memory/relational.py:1407-1417`; `web/src/components/investigations/SecurityReviewBanner.tsx:73-75` | — | — |
| F31 | Still partial | `agent/eoa/api/services.py:3065-3086,3171-3186`; `agent/eoa/orchestrator/main.py:118,157` | API calls share one lock, but scheduled runs use raw enqueue paths and can race an API enqueue. The lock holds only DB work, with no network I/O. | Route scheduled and API admission through one coordinated check. |
| F33 | Still partial | `agent/eoa/api/routes/payloads.py:65,78-85,93-117`; `web/src/pages/PayloadsPage.tsx:57-65,89-90` | Search and facets moved server-side, but results remain capped without pagination; API and tree search fields differ. | Align search semantics and paginate. |
| F35 | Still partial | `agent/eoa/fetch/service.py:719-730`; `agent/eoa/orchestrator/main.py:133-145` | Weekly retries and daytime hours improved; daily sources remain due on every two-hour poll. | Enforce the intended daily cadence. |
| F36 | Still partial | `agent/eoa/memory/relational.py:336-344,370-416`; `db/migrations/versions/0036_items_canonical_url_index.py:35-37` | Alias-first lookup is fixed, but an equally good redirect cannot establish a new canonical URL; later direct fetch can duplicate it. The index is nonunique and observed aliases are not retained. | Separate trustworthy identity updates from content-quality upgrades. |
| F40 | Still partial | `agent/eoa/report/acquisition_watch.py:139`; `agent/eoa/report/geography.py:268`; `agent/eoa/report/product_line.py:183,220,565` | These report buckets still cast timestamps using the DB session zone. | Anchor remaining casts to Jerusalem. |
| E01 | Still partial | `agent/eoa/pipeline/dedup.py:78-80,92-105` | A pending re-embed item’s stale vector remains in the candidate pool until its turn; an earlier item can match it. | Remove pending IDs before scoring or embed them first. |
| E05 | Fix is wrong | `agent/eoa/llm/ollama_client.py:526-552,911-923`; `agent/eoa/llm/chain.py:254-275` | Returned retry usage is aggregated, but nested calls and the outer aggregate are both logged, overstating cost. | Record each actual call once; keep aggregate return values separate from ledger rows. |
| N01 | Fixed | `agent/eoa/fetch/service.py:685-693,719-730` | — | — |
| N02 | Fixed | `agent/eoa/api/services.py:3317-3329`; `agent/eoa/memory/relational.py:1440-1445` | — | — |
| N03 | Fix is wrong | `agent/eoa/orchestrator/jobs.py:931-953`; `agent/eoa/memory/relational.py:1644-1652`; `agent/eoa/notify/ntfy.py:72-87` | A failed push returns `Sent(ok=False)`, yet `_notify` has already committed “sent” and ignores that result. Replay never retries. | Use a retryable delivery record and check the send result. |
| N04 | Fixed | `agent/eoa/fetch/service.py:323-353`; `agent/eoa/memory/relational.py:334-345,373-385` | — | — |
| N05 | Fixed | `agent/eoa/memory/relational.py:373-385`; `agent/eoa/memory/vector.py:187-205` | — | — |
| N06 | Still partial | `agent/eoa/pipeline/dedup.py:78-80,95-105` | Cutoff and replacement work after processing, but a pending item’s old vector can still cause a false link. | Fix the initial pool; test the earlier-item ordering. |
| N07 | Still partial | `agent/eoa/orchestrator/jobs.py:582-606,641-655`; `agent/eoa/memory/relational.py:1578-1608` | Three-hour fallback can duplicate an active daily pipeline; a deferred daily lets weekly build too early. | Recognize deferred as incomplete and never fall back while an active daily exists. |
| N08 | Fixed | `agent/eoa/api/routes/security_review.py:162-171`; `agent/eoa/memory/relational.py:1407-1417` | — | — |
| N09 | Fixed | `agent/eoa/memory/relational.py:370-382` | The reported *lower-quality blocked redirect* no longer changes canonical identity; R04 is a new same-quality regression. | Fix R04 without reopening the blocked-redirect case. |
| N10 | Fixed — performance needs confirmation | `db/migrations/versions/0036_items_canonical_url_index.py:32-37`; `agent/eoa/memory/relational.py:390-416` | The lookup is indexed and cooperating inserts serialize. No representative `EXPLAIN` or concurrent alias test was available; the index is nonunique. | Measure lookup cost and test concurrent aliases. |

## New issues in e3a5092

| ID | Severity | File:line | Problem | Evidence | Fix |
|---|---|---|---|---|---|
| R01 | P1 | `agent/eoa/fetch/html.py:263-280`; `agent/eoa/fetch/service.py:843` | TLS identity can be checked for the wrong hostname. | Both hosts’ requests become the same IP origin. Installed `httpcore` reuses by origin (`.venv/Lib/site-packages/httpcore/_async/connection_pool.py:304-323`) and applies SNI only when opening TLS (`connection.py:105-107,149-156`). | Pool separately by original hostname and pinned IP, or keep the hostname origin with a pinned dialer. |
| R02 | P1 | `agent/eoa/orchestrator/jobs.py:931-953` | Failed notifications become permanently suppressed. | The marker commits before send; `ntfy.send` can return `ok=False` without raising (`agent/eoa/notify/ntfy.py:72-87`). | Track pending delivery, check acknowledgement, and retry failures; define duplicate handling for crash-after-send. |
| R03 | P1 | `agent/eoa/orchestrator/jobs.py:579,641-655` | Weekly can run on incomplete daily data or start another daily pipeline. | `deferred` falls through as terminal; after three hours even `running` triggers `run_daily`. `tests/unit/test_jobs_status.py:224-240` explicitly expects that fallback. | Permit weekly build only after an explicit terminal state; never duplicate an observed active daily. |
| R04 | P1 | `agent/eoa/memory/relational.py:336-344,370-382,400-416` | A credible same-quality redirect loses canonical identity. | Canonical changes require a changed hash **and** strictly higher quality, so a full/clean A redirecting to full/clean B remains unlinked; direct B can insert separately. | Validate canonical identity independently of content acceptance while rejecting suspect redirects. |
| R05 | P1 | `agent/eoa/pipeline/story_clustering.py:264-280` | Historical story remapping has a two-commit crash window. | `bulk_set_story_ids` commits before `remap_story_roots`; retry sees changed recent roots and may not reconstruct the old-root map. | Apply both updates in one transaction. |
| R06 | P2 | `agent/eoa/api/routes/payloads.py:78-80`; `web/src/pages/PayloadsPage.tsx:89-90` | Payload search can show false empty results. | SQL excludes vendor and variant fields searched by `filterPayloadTree` (`web/src/lib/payloadFamilies.ts:128-156`); a notes-only SQL hit can then be removed by that tree filter. | Use one set of search fields and avoid filtering server-matched rows differently. |
| R07 | P2 | `agent/eoa/llm/ollama_client.py:526-552,911-923` | Structured retry costs are counted twice in the ledger. | Nested calls record usage, then the outer chain records their aggregate; `agent/eoa/memory/relational.py:1244-1258` sums both. | Keep one ledger row per actual call. |
| R08 | P2 | `agent/eoa/api/routes/security_review.py:162-168` | Concurrent approvals can exhaust the eight-connection pool while holding review transactions. | `ensure_context_he` obtains a second connection (`agent/eoa/pipeline/investigation_context.py:60`); the pool maximum is eight (`agent/eoa/db.py:20-24`). | Fetch context on the caller’s connection or before claiming the row. |
| R09 | P3 | `web/src/pages/PayloadsPage.tsx:77-80,144-145`; `web/src/pages/PatentsPage.tsx:71-77` | Some zero-match filters can be labelled “database empty.” | Existence is inferred from facet values or returned rows; records with null facet fields can make both empty under a filter. | Use an unfiltered total or explicit existence flag. |

## Must-fix before deploy

- Fix R01’s cross-host TLS reuse and add a two-host, same-IP transport test.
- Fix R02’s lost-notification path and test `Sent(ok=False)` followed by replay.
- Fix R03’s `deferred` handling and active-daily timeout fallback.
- Fix R04’s same-quality canonical redirect and E01/N06’s pending stale-vector matches; both can create lasting identity or dedup errors.
- Make F20’s current and historical story updates atomic.
- Include scheduled enqueues in F31’s run-admission invariant so an API request cannot race the scheduler.

## Can follow later

- Complete F05, F09, F26, F33, F35, and F40; correct E05’s cost ledger and R08–R09. Verify F06 with the installed Serve mapping. Migration 0036 uses an ordinary, nonunique index; production write-lock duration and lookup performance need confirmation.
- **Test discriminator check:** the new SQL-shape and behavior tests for N01, N04, N05, F11, F20, F24, and E05 would reject the earlier behavior. The PostgreSQL tests for N02, N04, N08, and N10 discriminate only when their DB prerequisite is available. F01’s test proves URL rewriting and the SNI extension but misses pooled TLS reuse; N03 misses failed delivery; N07’s timeout test endorses the unsafe branch; N06 misses the earlier-item ordering.