# תוכנית הסבה: EO-Analyst כאפליקציית Windows ללא Docker + תוכנית תיקונים משולבת

**תאריך:** 2026-09-05 ערב · **החלטת המשתמש:** מעבר מלא מ-Docker להתקנה מקומית, גם במחיר ירידה בבידוד הרשת. התיקונים מהמעבר המשותף ([REVIEW_2026-09-05.md](REVIEW_2026-09-05.md)) משולבים בתוכנית ומתבצעים במקביל.

## 1. עקרונות

1. **הכול בתיקיית הפרויקט.** PostgreSQL נייד, ntfy, Python 3.12, מודל ה-guard: תחת `runtime/` (לא ב-Program Files, ללא הרשאות מנהל). היוצא מן הכלל: משימה אחת ב-Task Scheduler ברמת המשתמש להפעלה אוטומטית בכניסה.
2. **תהליך אחד במקום שלושה קונטיינרים:** ה-agent מבצע קליטה בעצמו (הקוד כבר תומך ב-`EOA_ROLE=host`), ה-web רץ כ-uvicorn מקומי. גשר ה-jobs בין agent ל-fetcher (ADR-003) מתבטל.
3. **בלי הרחבות PostgreSQL:** pgvector מוחלף בחישוב cosine ב-numpy (מאות עד אלפי וקטורים בחלון של 7 ימים, טריוויאלי); Apache AGE מוחלף בטבלת `graph_edges` ב-SQL עם CTE רקורסיבי לשכנים בעומק N. ה-API של `eoa.memory.graph` נשמר זהה כדי שהקוראים (analyze, api, obsidian, monthly) לא ישתנו.
4. **חיפוש בלי SearXNG:** ספק חיפוש מובנה בפייתון (ספריית `ddgs`, מנועים מרובים: duckduckgo/bing/google/brave/yahoo) מאחורי ממשק `eoa.search.provider`; SearXNG נשאר ספק אופציונלי אם קיים URL.
5. **פיצוי אבטחתי לאובדן הבידוד** (ADR-004): נשארים שומרי L1 (ONNX) ו-L2 (LLM), מסגור DATA, SSRF guard, רשימת URL מותרת בחקירות עומק, deny-list דומיינים. נוסף: לוג יוצא של כל בקשת HTTP (host, bytes) לביקורת.
6. **Ollama** נשאר כפי שהוא (מקומי, GPU).
7. **מודלי ענן דרך CLI (U8)** הופכים פשוטים בסביבה מקומית: `agy` / `claude -p` / `codex exec` כתת-תהליך, opt-in מפורש, מסומן בממשק, לעולם לא בריצת הלילה.

## 2. ארכיטקטורה חדשה

```
EO-analyst/
  runtime/                 # לא ב-git
    pgsql/                 # PostgreSQL 17 binaries (EDB zip)
    pgdata/                # cluster, port 5433 (ללא שינוי ב-DATABASE_URL)
    ntfy/ntfy.exe + ntfy-cache/   # port 8090
    python/                # CPython 3.12 (uv python install)
    models/prompt-guard/   # ONNX של Protect AI (EOA_GUARD_L1_DIR)
    logs/
  .venv/                   # uv venv על 3.12, `eo` CLI
  web/dist/                # npm run build, מוגש על ידי FastAPI
  scripts/native/
    install_native.ps1     # התקנה חד-פעמית (הורדות, initdb, venv, build, מיגרציה)
    eoa-supervisor.ps1     # מפעיל ומנטר: postgres, ntfy, agent, web; restart on crash
    register_autostart.ps1 # Task Scheduler "EO-Analyst Supervisor" (at logon, user level)
    migrate_from_docker.ps1# pg_dump מהקונטיינר → restore מקומי (חד-פעמי)
```

תהליכים: `pg_ctl` (postgres) · `ntfy.exe serve` · `python -m eoa.orchestrator.main` (scheduler+worker+fetch) · `uvicorn eoa.api.app:app --port 8765`.

## 3. שלבי הביצוע

| שלב | תוכן | תלות | מבצע |
|---|---|---|---|
| 0 | תוכנית זו; פתיחת סוכני גל 1 | — | Fable |
| 1a | vector.py → numpy + `embedding real[]` (alembic 0006); graph.py → SQL `graph_edges` באותו API; סקריפט ייצוא קשתות AGE → graph_edges | — | סוכן M1 |
| 1b | `eoa.search.provider` (ddgs + searxng אופציונלי); החלפת קריאות ב-deep_search, tenders, conferences | — | סוכן M2 |
| 1c | סקריפטי runtime (install/supervisor/autostart/migrate), `eo start/stop/status` מקומי, תיקון ntfy (F11), תאימות Windows (signals, paths), ADR-004 | — | סוכן M3 |
| 2 | הורדות (באישור): PostgreSQL 17 zip, ntfy.exe, CPython 3.12 (uv), מודל guard; initdb; venv; npm build | 1c | Fable + M3 |
| 3 | מיגרציית נתונים: alembic 0006 על DB הקונטיינר → ייצוא קשתות → pg_dump (ללא ag_catalog) → restore מקומי; אימות ספירות | 1a, 2 | Fable |
| 4 | הפעלה מקומית מלאה; `eo status`; בדיקות אינטגרציה חיות; e2e Playwright מול 8765 המקומי | 3 | Fable + סוכן QA |
| 5 | כיבוי Docker (containers/images; ה-volume נשמר עד אישור מחיקה); docs: RUNBOOK, USER_GUIDE, README, STATUS, NIGHT_CHECKLIST; רישום autostart | 4 | Fable |

## 4. תוכנית התיקונים המשולבת (מ-REVIEW_2026-09-05)

| חבילה | פריטים | מבצע | גל |
|---|---|---|---|
| A1 מכרזים | F1 (קטימת פרומפט תחזית → הגבלת DATA, num_ctx של summarize, שומר פלט), F2 (חילוץ תאריך פרסום/דדליין/גורם מזמין מעמוד ההודעה, סטטוס unknown ללא תאריכים, סגירה אוטומטית > 365 יום), F13 (מדינה) | סוכן | 1 |
| A2 דוחות | F3 (חלון 24 שעות אחורה), F4 (weekly לא מריץ דוח יומי כפול), F5–F8 (תקציר לא חוזר, מכרזים פעם אחת, כותרת עם גרשיים, שם מקור), F9/F16 (dedup אירועים + תאריכים), F10 (docx: ללא TOC-placeholder, היפר-קישורים, כותרת רצה), U13 (שבועי), U1 (HTML נקי, RTL תקין) | סוכן | 1 |
| A3 הבוקר/הפעלה | F12 (KPI נכונים), U2 (כרטיסים לחיצים), U3 (הפניות [n] מקשרות לפריט/מקור), U4 (הרץ עכשיו: מניעת כפילות, מצב ריצה, ETA, פס התקדמות, ג'ובים ברקע), F17 | סוכן | 2 |
| A4 שפה/גאוגרפיה | U5 (הנחיות בלשון אנושית, שפה אחת), U6 (בורר עברית/אנגלית), U7 (סינון/ריכוז לפי מדינה, ניתוח פר-מדינה בדוח) | סוכן | 2 |
| A5 צ'אט/חקירות | U9 (פריט בהקשר נכנס לתשובה; פריטים חסומים/403 לא נכנסים ל-RAG), U11/U12/F18 (ניסוח שאלות חקירה, "המשך חקירה" מובן, ביטחון על not_found) | סוכן | 2 |
| A6 ישויות | U10 (עיצוב מחדש: רשימה + כרטיס ישות + גרף קומפקטי, קישורים עובדים), F15 (סינון ישויות לא רלוונטיות) | סוכן | 2 |
| A7 מודלי ענן | U8 (ספקי CLI: agy/claude/codex; בורר בהגדרות ובצ'אט; אינדיקציה; ברירת מחדל מקומי) | סוכן | 2 |
| A6b ישויות | ציון רלוונטיות מתבסס רק על items.entities_mentioned (מאויש ב-32 פריטים בלבד) → ישויות אמיתיות (US Navy, Rolls-Royce, Air Force) מקבלות 0 ומוסתרות; לשקלל דרגת קשתות בגרף, השתתפות באירועים, סוג org/agency; ריצה מחדש של backfill | סוכן | 2 |
| A8 MCP | שכבת לקוח MCP ב-agent (שרתים לקריאה בלבד, allow-list בהגדרות, מסגור DATA לפלט): (1) שרתים שכבר מחוברים למשתמש — פיננסי (SEC 8-K/10-K/20-F, תמלילי רווחים, חדשות) לחברות ה-watchlist, ומחקר אקדמי (SPIE/arXiv); (2) שרת MCP משלנו לרכש: SAM.gov API, USAspending (קודי PSC אופטיקה/IR), DSCA, Federal Register, Congress.gov; (3) **Janes — למשתמש יש מנוי (אושר 2026-09-05):** שרת MCP עוטף את Janes Data Services API (equipment, news, markets, budgets, events) עם JANES_API_KEY מ-.env שהמשתמש מזין בעצמו; ללא API — אין אוטומציה מול הפורטל; (4) פטנטים: EPO OPS / PatentsView / Lens; (5) חיפוש משני: Brave free tier. במצב ענן ה-CLI מקבלים את אותם שרתי MCP בקונפיג שלהם | סוכן | 3 (אחרי A5/A7) |
| A11 דוח פיתוח עסקי לטריטוריות | **דרישת המשתמש 2026-09-06:** דוח מיקוד למשימות פיתוח עסקי, מכירה ושיווק לפי טריטוריה (מדינה/אזור): תמונת שוק (פלטפורמות ורכש אחרון, תוכניות, תקציבים), מכרזים/RFI פתוחים ותחזיות, מתחרים פעילים וזכיות, כנסים קרובים בטריטוריה, שותפים/סוכנים אפשריים, נקודות כניסה ופעולות מומלצות עם עדיפות; שבועי + לפי דרישה; docx/md/html; מסך "פיתוח עסקי" עם בחירת טריטוריה ותקופה | סוכן | 3 |
| A12 מעקב טכנולוגי | **דרישת המשתמש 2026-09-06:** אין התייחסות להתפתחויות טכנולוגיות (דוגמה: פרסומים מדעיים על FPA עם פיקסל דיגיטלי). להוסיף דומיין טקסונומיה "התפתחויות טכנולוגיות" עם תתי-תחומים (DROIC/פיקסל דיגיטלי, SWIR/eSWIR, HOT MCT/T2SL, גלאים מבוססי אירועים, אופטיקה מטא-משטחית, עיבוד על-החיישן ו-AI קצה, לייזרים/LiDAR, ראייה ממוחשבת לזיהוי מטרות), מקורות אקדמיים ומקצועיים (arXiv eess.IV/cs.CV, SPIE Digital Library/JEI RSS, Optica, IEEE Sensors/TGRS, DTIC/OSTI, Laser Focus World, Photonics Spectra, פטנטים דרך MCP), שלב סיווג טכנולוגי (TRL, בגרות, שחקן), סעיף "מעקב טכנולוגי" בדוחות היומי/שבועי, ומסך "רדאר טכנולוגי" | סוכן | 3 |
| A13 מיקוד תעשייה ישראלית | **דרישת המשתמש 2026-09-06 בוקר:** חברות ביטחוניות ישראליות, ובפרט בהקשרים אלקטרואופטיים, חייבות מיקוד. (1) הרחבת watchlist: Elbit/Elop/Elisra, Rafael (Litening, Toplite, Iron Beam, Drone Dome), IAI/ELTA/Tamam (MOSP, POP), Controp, SCD (גלאים), Ophir Optronics (MKS), Opgal, Nextvision, Netline, Smart Shooter, Xtend, D-Fend, UVision, Aeronautics, Steadicopter, Third Eye, Sightec, Camero, Meprolight, Duke Robotics, BIRD Aerosystems, Israel Weapon Industries (EO sights), Tomer, RADA (Leonardo DRS), משרד הביטחון/מפא״ת/סיב״ט; (2) דגל `israel_relevance` בסיווג ורכיב ציון במיון (מעורבות חברה ישראלית, לקוח ישראלי, מתחרה ישיר לחברה ישראלית, שוק יצוא של ישראל); (3) סעיף "תעשייה ישראלית" בדוח היומי והשבועי (זכיות, תחרות מול ישראליות, הזדמנויות יצוא, איומים רגולטוריים/מדיניים); (4) מסנן/צ'יפ "ישראל" בפיד ובישויות, `entities.is_israeli`; (5) מקורות עבריים/ישראליים נוספים (Israel Defense, כלכליסט ביטחון, גלובס ביטחון, ynet/מעריב ביטחון, דובר משרד הביטחון, SIBAT, IsraelDefense.co.il EN); (6) דוח BD מנוסח מפרספקטיבה ישראלית כברירת מחדל; (7) חקירות עומק: שאלות "מה המשמעות לתעשייה הישראלית" | סוכן IL1 | 3 |
| A14 פרק אנליסט: פטנטים ו-IP | **דרישת המשתמש 2026-09-06:** לא היה מאופיין. אפיון: (1) איסוף: EPO OPS + PatentsView (מפתחות חינמיים, דרך שרת ה-MCP הקיים) + Google Patents דרך ספק החיפוש כגיבוי; מסננים לפי CPC של EO/IR/CV (G01J5, G02B23/27, G01S17, H04N5/33, H04N23, H01L27/146, F41G3/7, G06T7, G06V20) ולפי מקצה (watchlist + חברות ישראליות); טבלאות `patents`, `patent_families`, `patent_watch_topics`; (2) ניתוח לכל פטנט: תקציר תביעות בעברית, תת-תחום, בגרות/TRL, קישור לישות (assignee→entities), הקשר עסקי (תוכניות/מוצרים/שותפויות של המקצה מהמאגר), מיפוי תחרותי לפי טכנולוגיה; (3) מדדי ערך פרוקסי (לא הערכת שווי כספית): גודל משפחה, מספר טריטוריות, ציטוטים קדימה, שנות חיים שנותרו, ליטיגציה ידועה, קצב הגשות של המקצה — ציון 0–100 עם הסבר; (4) סקר פטנטים לפי דרישה ("סקר פטנטים: FPA פיקסל דיגיטלי") כסוג חקירה חדש שמייצר docx: נוף, שחקנים, מגמות בזמן, חורים/הזדמנויות, המשמעות לתעשייה הישראלית; (5) דוחות: סעיף "פטנטים ו-IP" שבועי (הגשות/הענקות חדשות בתחום, מקצים בולטים) וחודשי (נוף); דוח BD: עמדת IP של מתחרים בטריטוריה; (6) מסך "פטנטים": הגשות אחרונות, רדאר CPC × מקצה, מדדי ערך, מפעיל סקר; (7) משימה שבועית `patent_scan`; ללא מפתחות — המודול פעיל במצב "ממתין למפתחות" עם הודעה ברורה ומצב מדגם | סוכן A14 | 3 |
| A14b סקר פטנטים — עומק אנליטי | **דרישת המשתמש 2026-09-06 (סקר FPA):** (1) **מפה טכנולוגית בשפת בני אדם**: לכל אשכול (CPC/נושא) פסקה: מה ההתקדמות שהפטנטים מתארים, מה המשמעות הטכנולוגית (ביצועים, עלות, ייצוריות, יישום מבצעי), לאן המגמה הולכת ומה המפה מראה (מי מוביל, איפה מתרכזים, מה דליל); (2) **ניתוח אשכולות**: זיהוי אשכולות לפי CPC + מילות מפתח + ציטוטים, גודל וקצב הגשות לכל אשכול, מקצים דומיננטיים, בגרות, קשרים בין אשכולות; (3) **יחסים עסקיים**: מקצים משותפים, שיתופי פעולה/רישוי/רכישות מהמאגר והפטנטים (co-assignment, ציטוטים צולבים), שרשרת ספק–אינטגרטור–לקוח, קשר למוצרים ותוכניות; (4) **לכל פטנט מצוטט**: תיאור קצר (2–3 משפטים בעברית) של ההתקדמות המתוארת — מה הבעיה, מה הפתרון, מה חדש — כחלק מנספח הפטנטים ובגוף הדוח בהפניה; (5) תקציר מנהלים שמסכם את המפה, לא רק ספירות; (6) **ציר זמן** (דרישה 2026-09-06): התפתחות על פני זמן — מה הופיע ומתי (תאריכי עדיפות/הגשה/פרסום/הענקה לכל פטנט ולכל אשכול), גלים של הגשות לפי שנה ומקצה, מתי כל פטנט צפוי לפוג (20 שנה מתאריך ההגשה, בכפוף לתחזוקה; סימון "פג/עומד לפוג ב-3 שנים הקרובות"), מה נכנס לנחלת הכלל ומה זה מאפשר, ופטנטים חדשים שעדיין בבחינה — כטבלה וכגרף ציר-זמן (md/html/docx) | סוכן P3 (אחרי P2) | 3 |
| Q | F14 (מקורות שנכשלו), בדיקות, e2e, סקירת Codex על ה-diff | Fable/QA | 3 |

## 5. סיכונים והחלטות

- **onnxruntime/torch על Windows 3.12:** גלגלים קיימים; אם optimum בעייתי — נופלים ל-onnxruntime ישיר עם tokenizers.
- **ddgs נחסם/CAPTCHA:** ספק עם כמה מנועים + backoff; אפשר להשאיר SearXNG כ-fallback ב-WSL בעתיד.
- **ניידות PostgreSQL:** zip רשמי של EDB, ללא installer; `pg_ctl` מהסופרווייזר.
- **הרשאות:** ללא admin; Task Scheduler ברמת משתמש. NSSM קיים במחשב כאופציה (דורש admin).
- **Docker volume:** נשמר עד אישור המשתמש למחיקה; pg_dump נשמר ב-`output/backups/`.
