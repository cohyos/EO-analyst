"""Export layer: turns the relational/graph memory into human-browsable output.

Currently one exporter: :mod:`eoa.export.obsidian` (FR-6.5), which writes a
plain-Markdown Obsidian vault under ``config.export.obsidian.vault_dir``.
"""

from __future__ import annotations
