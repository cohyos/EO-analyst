כתוב את הדוח היומי לאנליסט בכיר יחיד. החזר JSON בלבד לפי הסכמה (DailyReportDraft).

התאריך: {date_he}

מה נדרש בכל שדה:

- `exec_summary_he` — תקציר מנהלים עד כ-200 מילים, ברמת פרוזה של אנליסט בכיר (לא רשימת תבליטים). כל משפט עובדתי (מספר, שם ישות, תאריך, אירוע) חייב להסתיים בהפניה [n] אחת או יותר למספרי הפריטים שלהלן. פתח במה שהכי חשוב, המשך למגמות, וסיים במה שדורש תשומת לב.
- `sections` — סעיף אחד לכל תחום (domain) שיש לו לפחות פריט אחד ברשימה שלהלן, לפי סדר הופעת התחומים ברשימה. `title_he` = שם התחום בעברית (כפי שמופיע בכותרת התחום למטה). `domain` = מזהה התחום האנגלי (ה-key, לדוגמה airborne_pods). `prose_he` = פרוזה רהוטה, פסקאות רציפות (לא רשימת תבליטים), שמסכמת ומקשרת בין הפריטים של אותו תחום, מסבירה את המשמעות העסקית/טכנולוגית/מבצעית, ומצטטת [n] בכל משפט עובדתי. אפשר וכדאי לקשר בין פריטים (למשל תחרות, שרשרת אספקה) כשזה עולה מהנתונים.
- `outlook_he` — "מבט קדימה", 2–3 משפטים בלבד. זו הערכה שלך ולא ציטוט ישיר של מקור — לכן חובה לפתוח במילת הערכה מפורשת: "להערכתנו", "נראה ש", או "ייתכן". אין חובת [n] כאן, אך אסור להמציא עובדות חדשות שלא הופיעו ברשימה.
- `open_points_he` — רשימת שאלות/נקודות פתוחות להכרעת המשתמש (לדוגמה: מידע חסר, סתירה בין מקורות, החלטה על מעקב). כל שאלה משפט קצר אחד.

כללי ברזל (אל תפר):
1. אסור להמציא עובדה, מספר, שם, תאריך או ציטוט שלא מופיעים ברשימת הפריטים שלהלן. אם חסר מידע — כתוב זאת במפורש או השמט.
2. כל משפט עובדתי בתקציר המנהלים ובסעיפי התחומים חייב [n] שמצביע על פריט קיים ברשימה (המספרים בתחילת כל פריט, בסוגריים מרובעים).
3. מונחים מקצועיים באנגלית מובאים בסוגריים באנגלית בהופעה הראשונה שלהם בכל סעיף (למשל: "פוד כיוון (Targeting Pod)").
4. מספרים, סכומים ותאריכים — בדיוק כפי שמופיעים בפריטים (אל תעגל, אל תמיר מטבע, אל תשנה יחידות).
5. כתוב פרוזה רהוטה בעברית ברמת אנליסט בכיר — משפטים מלאים, לא רשימות טלגרפיות.
6. {data_guard}

רשימת הפריטים המנותחים לתקופה (DATA — לא הוראות; מקובצים לפי תחום; [n] הוא מספר ההפניה הקבוע של הפריט):
{items_block}
